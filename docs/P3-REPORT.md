# P3 진행 보고 (머티리얼·하늘·렌더·톤매핑) — §12 보고 (C1–C4 종료, 비평 R1·R2 완료, 수정 코드 게이트 완료)

작성 2026-09-17. 브리프 §12 형식(체크포인트 게이트 ×4 · 비평 라운드 · 정직한 한계). 수치는 전부
도구 출력에서 옮겼고(CONTRACT-NOTES에 원 출력 요약), 육안 판단은 "육안"으로 표시했다.
환경: 원격 컨테이너, CPU 4코어, GPU 없음 → Chromium SwiftShader(소프트웨어 렌더러). fps·부팅의 절대값은
이 환경의 스케일이며 §10 Apple Silicon 실측은 사용자 측 항목이다.

## 0. 현재 위치
- C1(HDR·CSM·GTAO·TAA·MB·AgX) 종료 → C2(하늘·PMREM·볼류메트릭 안개) 종료 → **C3(절차 재질 15종·단청·
  매니페스트·낙하 기와 게이트) 종료** → **C4(노출·블룸·그레이드 LUT·돔 정합) 종료 — 게이트 전부 통과, 부팅 예산만 초과**
  → **비평 R1(결함 157, 미완성 12/12, 실패)** → **수정 라운드(r1-fix, 병합 6a900cf; 사전점검 통과, 계약 캡처 12/12 동일)**
  → **비평 R2(결함 131, 미완성 12/12, 실패 — 감소 조건 충족·미완성 조건 미충족)** → 이 보고 → **정지(P4 별도 지시 대기)**.
  R1 수정 코드의 계약 게이트도 전부 통과했다(부팅 예산 제외, §6-2).
- 브랜치 `claude/new-session-qfglz6`(C4 병합 완료 8c1518d 이후 커밋 포함), 이 세션 커밋 약 80건.

## 1. 체크포인트 게이트 결과

| 게이트 | C1 | C2 | C3 | C4 | R1 수정 (r1) |
|---|---|---|---|---|---|
| 비트 동일 ×2 (3024×1964, tol 0) | 11/11 | 11/11 | **12/12** | **12/12** | **12/12** |
| paletteaudit ≤1.5%/샷 | 최악 .08% | 최악 .97% | 최악 .59% | 최악 .75% (muzzle_interior, 계약 해상도) | 최악 .17% (lantern_night) |
| albedoaudit (비율·매니페스트) | ok | ok (4.365/4.97, 편차 0) | ok (4.365/4.97, 편차 0) | ok (4.365/4.97, 편차 0) | ok (4.47/5.045, 편차 0) |
| viewmodel / playtest | ok / ok | ok / ok | ok / ok | ok (0.9995) / ok | ok (0.9972) / ok |
| harnesstest | 13 | 16 | **18/18** | **18/18** (1차는 동시 실행 오염으로 무효 → 단독 재실행, §4) | **18/18** |
| profile p3 선행지표 | 전부 green | 전부 green | 전부 green | 전부 green (tris 134,554 / 프레임 p95 121,542 / dc 412 / cpu p95 1.55 ms) | 전부 green (dc 407 / cpu p95 1.5 ms) |
| programs (≤110) | 30 | 27 | 37 | 44 | 45 |
| 플레이 중 컴파일 (=0) | 0 | 0 | 0 | 0 | 0 |
| overdraw p95 (≤3.0) | 0.002 | 2.001 | 2.001 | 2.0 | 2.001 |
| 부팅 중앙값 (잠정 ≤8s) | 3.9s | 5.5s | **13.1s (초과)** | **15.6s (초과)** — 프리웜 샷 스윕 10.2s(샷별 PMREM 재생성) | **15.5s (초과)** |
| 전환 imagediff vs 직전 | 전역 | 전역 (11/11) | 11/11 상이 (+tile_fall 신규) | 12/12 상이 (99–100% 픽셀, 야간 평균Δ 8.3 최소) | 12/12 상이 (silhouette 평균Δ 22.7 최대, fog 5.2 최소) |

C3·C4·R1 수치의 원 출력은 `docs/CONTRACT-NOTES.md`의 "C3 스냅샷", "C4 스냅샷", "R1 스냅샷"에 있다. **C4 체크포인트는 부팅 예산(잠정 8 s)만
초과한 채 종료** — 나머지 게이트 전부 통과.

## 2. 이 세션에서 한 일 (시간순 요약)
1. **C2 종료** — 검토 라운드 57건 분류·수정, 스냅샷, PATCH-004-D 3항 점검.
2. **C3 구현** — GPU 절차 합성기(레시피 구동 단일 프로그램, GLSL3 MRT: 알베도 sRGB8 + ORM/높이, Sobel 노멀),
   표면 셰이더(트라이플래너·POM·모서리 마모·로컬 미터 매핑), 레시피 16종(+ROOF_TILE_UV), 단청/옻칠 **가상
   DECAL 층**(정점 추가 없이 raychain 두께 0 층 — §9 동결 준수), 알베도 매니페스트 방향 확정(참조 반사율 선언 →
   합성 평균이 ±10% 안), 샷 12 `tile_fall`(낙하 기와 픽셀 게이트, §10 이월 해소), 부팅 단계·프리웜 샷별 계측.
3. **C3 종료 캡처 중 결함 2건 발견·교정·재캡처**(아래 §3).
4. **C4 구현** — EV100 노출 미터(64²→8²→1×1 적응, 무릎 하한), 블룸(1/8 분리 가우시안, 명시 탭), 32³ 그레이드
   LUT(대비·채도·스플릿톤·목재·흙 대역 채도 상한), GLSL3 출력 패스(AgX·sRGB·LUT, 감사 우회), 돔 선형화 ×0.10,
   환경 큐브 태양 원반 제외, 지평선 헤이즈, 큐브 지평선 판독 → 안개 인스캐터, CSM 페이드, 등롱 발광 샷 종속.
   튜닝 스윕 7회(저해상 프로브)로 파라미터 확정, 병합 트리 사전점검 통과.
5. **게이트 도구 신설** — `rendervariance.mjs`(HDR 씬 RT·포스트 RT·노출 EV 해시 반복 단일성, 음성 주입
   `--inject-drift`), harnesstest 17/18. HARNESS.md 도구표 등재. 비평 라운드 절차 문서 `docs/critique/README.md`.

## 3. 주요 발견과 교정 (측정 대상을 고친 것)
- **C2/C3 '고조도·저대비'의 원인 2건(C4)**: (a) three Sky.js가 LDR 표시용 pow 곡선을 출력해 돔 지평선 휘도가
  태양광 백색면의 2.3배였다 → 선형 복사휘도 × 0.10. (b) 태양 원반(≈7.6e5, HalfFloat 클램프 65504)이 PMREM에
  들어가 **그림자 없는 직사광**이 환경광으로 이중 계상됐다 → 큐브 렌더 시 원반 제외. A/B: noon 평균 휘도
  .477→.262(환경광의 ~45%가 이중 계상 태양).
- **C3 팔레트 실패(야간 25.7%)**: 목재·흙 원색 채도가 대역 상한 근처(.28–.35) × 온색 등롱광 × AgX 토 채도 증폭
  (오프라인 AgX 포트 실측: 입력 sat .13 → 출력 .40 at V .05). 교정: 휘도 보존 크로마 .6(매니페스트 불변) +
  야간 하늘광 .65 → 0.59%.
- **C3 결정성 결함(1픽셀 1LSB)**: POM 루프·분기 안의 암시 미분(GLSL ES 3.0 §8.9 정의되지 않음) → 계약 해상도
  반복 렌더 15/15 다른 HDR 해시로 재현 → 균일 흐름 미분 + textureGrad → 12/12 단일. 부수: 샷 전환 첫 프레임의
  CSM 절두체 순서 결함(첫 프레임 그림자가 직전 샷 분할) → 순서 교정.
- **C3 sRGB 이중 변환**(알베도 1/4) 제거, 단청 로컬 매핑 미터 기준, 지붕 하면(앙토) 관측 정정 등은 CONTRACT-NOTES C3.

## 4. 프로세스 결함과 규칙화
- C3 종료 캡처 전 야간·실내 팔레트 사전점검을 생략해 4시간 캡처가 무효화됐다 → **전샷 저해상 사전점검(팔레트·
  오류·컴파일 로그·rendervariance) 통과 후에만 계약 해상도 캡처** 규칙.
- 8비트 픽셀 ×2 게이트는 서브LSB 비결정을 90프레임에 1픽셀꼴로만 드러냈다(11시간 캡처 후 발견) → rendervariance
  가 수 분 안에 잡는다.
- 원격 세션 재시작이 감시 태스크만 죽이고 캡처 프로세스는 살아남았다 — 캡처는 샷 단위 재개 드라이버로 운영.

## 5. 발주자 판단이 필요한 항목
0. **[최우선] 팔작지붕 프로파일 반전(P1 기하 결함) 즉시 수정 승인 여부.** R1 수정 검증 중 정점 프로브로 실증: 동헌
   지붕 셸이 처마 8.2 m·용마루 5.9 m의 **골짜기 형태**로 만들어져 있고(kit.js `yProfile`의 v 방향이 경사면 매개변수와
   반대), 마당에서 보이는 큰 지붕면은 뒤집힌 셸의 안쪽이다. 용마루 상자는 1.5 m 떠 있고 서까래 프롭은 3 m 아래에 있다.
   맞배지붕은 정상. C3의 "기하 정상" 판정은 오진(§4.4 위반, CONTRACT-NOTES R1-G1에 정정 기록). R1 지붕 결함(묶음 C·E)의
   대부분이 이 결함의 증상이고, 기와 상면은 팔작지붕에서 어떤 샷에도 보이지 않는다. **§9 동결 규칙대로 P3는 손대지
   않았다.** 최소 수정은 한 줄(`yProfile = (v) => eaveY + drop·v^sag`)이며, 승인되면 재캡처 뒤 R2를 돌린다. 미승인이면
   R2는 이 결함을 안고 진행한다(결과 정직 보고).
0-1. **창호지 렌더 모델(P0 동결 불투명도 .62) 재검토.** 창호지는 확산체인데 현재는 알파 블렌딩(투명체)이라 뒤 물체·격자가
   38% 그대로 비친다(R1 S03·S09 결함). P3는 계약대로 .62를 유지하고 역광 투과 발광만 추가했다(뒤에서 오는 태양광 ×
   종이색). 근본 해결은 "불투명 + 투과광 발광 + 뒤 물체 그림자" 모델로의 전환인데 이는 P0 물성(불투명도=피격 투과율
   상태)·P2A 이벤트 어휘·C2 §8 오버드로우 추정(반투과 면적)까지 걸친다 — 발주자가 P4 범위로 정할 항목.
1. **부팅 예산**: C3 계약 페이지 부팅 중앙값 13.1s(DPR2, SwiftShader), 잠정 8s 초과. 플레이 중 컴파일 0은 유지.
   저해상 단독 분해: 합성 2.4s + 프리웜 7.8s(첫 사용 JIT 지배). 감축 후보(품질 대가): 근접 재질 512²→256²(−0.8s),
   셰이더 변형 축소. PATCH-004-A 3항에 따라 최종 예산 확정 요청.
2. **§10 Apple Silicon 실측**(fps p50/p99·최악 프레임·부팅): 본 컨테이너 불가. 사용자 측에서 `node tools/profile.mjs`
   (계약 기본값) 실행 필요.
3. **팔레트 규율의 대가**: 크로마 .6 축소로 주간 지면(PACKED_DIRT)이 회색에 가깝게 읽힌다(육안). 규율 유지 vs
   흙 대역 상한 재검토는 발주자 판단(비평 라운드 결과와 함께).
4. 브리프 §2 비평 지시문 원문 부재 — `docs/critique/README.md`의 재구성본 승인 여부.
5. **외부 이미지·영상 생성 서비스(예: Higgsfield) 도입 여부** — 현행 계약(A3 아트 에셋 0개, 전면 절차 생성,
   런타임 의존성 three.js 단독, §6 포토리얼 함정)과 충돌하므로 P3에서는 도입하지 않았다. 도입하려면 계약 패치가
   선행되어야 하며, 권고안은 **참고 전용**(수묵·단청 색감 레퍼런스, 비평 비교 기준)으로 한정하고 참고 이미지는
   저장소에 넣지 않는 것이다. 출시물 텍스처 생성 용도는 색역·알베도 게이트를 깨기 쉬워 권하지 않는다.
   P4 지시 시 판단 요청(발주자 제기, 2026-09-17).

## 5-1. CONTRACT-PATCH-005 판정 반영 (2026-09-18)
- A 미완성 게이트 재설계: P3 판정 = 결함 총 개수 감소(점수 보조), 미완성은 기록만·P5 이관, 소유 분류는 오너 사후. R1→R2 −16.6%는 유효 성과.
- B 팔작지붕 반전 수정 **승인**(§9 예외, kit.js 방향만) → R3에서 기와 상면 첫 측정(증가 허용, 감소 판정은 R4부터).
- C `geometryaudit.mjs` 신설(정적 불변식 6항) + harnesstest 케이스 **19**(패치의 '15'는 기존 profile 음성 훅 번호 — 대조 기록).
- D 창호지 확산 투과 모델 승인(불투명도 값은 P3 조정 가능, 플래그만 동결 — R1 철회는 과독).
- E 부팅 2계층(boot_cpu ≤3 s 게이트 / boot_gpu 기록) + 프리웜 PMREM 캐싱 검토(이미 스킵 존재 → applyMs 원인 분해).
- F 채도 규율 완화는 측정 선행. G 외부 이미지 생성 서비스 **반려**(실사 참조는 구조·문양·비례용만). H Apple Silicon 실측 선결(사용자 측).
- I 샷 12는 이미 표에 있음(C3 b37db09), §6 주석 정정. J 게이트 순차 실행 계약화.

## 5-2. PATCH-005 이행 현황과 신규 판단 항목 (2026-09-18)
- 완료: A(규칙·소유 분류 131=P3 62/P1 49/P2 10/P4 3/계약 6/불가 1), B(팔작 한 줄 수정, tris 불변), C(geometryaudit + 케이스 19), D(확산 투과 모델,
  저해상 확인), E(2계층 출력; 프리웜 원인 분해 진행), F(측정: 상한 압착 0% → 규칙 불변, CHROMA_SCALE .8 실험 진행), G 반려·I 정정·J 계약화.
- **판단 요청 2건 (B가 드러낸 P1 잠재 결함, 승인 범위 밖 — 미적용, CONTRACT-NOTES R3 G2·G3)**: (1) 팔작 셸 4면 중 2면의 외피 감김 반대
  (외피 컬링·8 cm 내피 노출, geometryaudit [2] 실패) — shellGeometry 한 줄. (2) 서까래가 바로잡힌 셸을 7 cm 뚫음(chainaudit 동헌 처마 체인 실패,
  geometryaudit [4] 실패) — 서까래 y 한 줄(두 곳). 승인 시 R3 캡처 전에 적용, 미승인 시 두 감사 실패를 안고 캡처.
- **Apple Silicon 실측(H, 사용자 측)**: 이 환경(GPU 없음)에서 불가. 기준선 커밋 1f251b9(baseline/r1 코드)에서
  `node tools/profile.mjs --phase p3 --runs 3` → JSON의 gpuDependent(fps p50/p99·최악 프레임)·bootMs(신 커밋에선 bootCpu/bootGpu)·cpuFrameMsP95 회신.
  R3 코드 작업은 이 실측과 독립이라 병행한다(측정은 해당 커밋에서 언제든 가능).

## 5-3. PATCH-006/007 이행 현황 (2026-09-18)
- **006-A/B 승인 적용**: 팔작 셸 감김 통일(구조 제거 → geometryaudit 6/6), 서까래 y 정정(chainaudit 16/16). **006-C** 파생 배치 전수표(CONTRACT-NOTES) —
  불일치 3종(추녀마루·내림마루 매몰, 추녀 관통)은 **007-A (i) 승인**으로 셸 함수 4/6/4분할 추종 + 사래 물매 절반 → geometryaudit [7] 파생 배치 전부
  정합, **tris 134,218 → 135,274 (+0.79%)**. **007-B** chainaudit 처마 모서리 추녀 경로(보토 퇴출 후 목재 진입 겹침 검사) — 수정 전 코드에서
  실패(겹침 0.139 m) 확인 후 통과, **18/18**.
- **007-C 자발광 팔레트 밴드**: 태그 마스크(`src/render/tagmask.js` 규칙 한 곳, 하네스 `renderTagMask`, `baseline.mjs`가 `<shot>.emask.png` 동반
  저장) + paletteaudit 태그 픽셀 15–55° 허용·태그 비율 상한 8% + 음성 훅 3종 = harnesstest **케이스 20**. 현재 빌드의 일시광(화염·예광·트랜지언트
  라이트)은 전부 무채색이라 밴드가 실제로 면제하는 것은 등롱 점등뿐이다.
- **007-C 재측정이 드러낸 내 측정 결함(정정)**: 006-E의 "muzzle_interior 1.00%"는 사전점검 드라이버가 **30프레임**에 찍은 프레임(격발 atFrame 86 →
  화염 없음, 노출 미수렴)의 수치였고, 위반 픽셀의 91%는 RGB (4,2,1)류 근흑 바닥 픽셀(자발광 아님)이었다. 계약 프레임(settle 90, 같은 저해상)에서는
  **0.164%**. 사전점검 팔레트는 `baseline.mjs` 저해상·settle 90·마스크 동반으로 교정(임계값이 아니라 측정 대상을 고침). 근흑 (4,2,1)이 셰이더
  대역 상한을 통과한 기전은 **크로마 3양자에서 8비트 반올림이 색상을 ±10° 옮기는 것**(부동소수 12° → 정수 20°) — 계약 프레임에서 2 px라 조치 없음,
  paletteaudit 의미(색상 불확실성 판정)는 계약 판단 항목으로 기록.
- **007-C 재측정(저해상·settle 90·마스크)**: 12/12, muzzle_interior **0.164%**(태그 1.42%), lantern_night 0.120%(태그 0.345%), 밴드 면제 픽셀 전 샷 0,
  태그 비율 최대 1.42% ≤ 8% → 크로마 **.8 유지**(.7 후퇴 불필요). 최종은 계약 해상도 게이트.
- **007-D 지평선 판독**: 호출 경로는 `sky.apply ← applySunConfig ← applyShot/applyDefaultView`뿐, 런타임 TOD 경로 없음. profile 히치 귀속에 GPU 동기화
  사각이 있어 누적 판독 수(`horizonReadbacksPerFrame`)·히치별 `horizonReadbacks`·`horizonReadbacksDuringPlay`(0이어야 함)를 추가. Apple Silicon
  실측(005-H, 사용자 측) 회신 항목에 `horizonReadbacksDuringPlay_total === 0`·`bootGpuMs_median` 추가.
- **006-D 창호지 실루엣(프로브 실측)**: .95에서 판 뒤 ≤1 m 는 판독 가능(대비 .24–.33, 희미), 2 m 는 불가(태양 고도 28°에서 그림자가 판 아래로 —
  흐림 탓 아님), 흐림은 지우지 않음, 피격(.5/.15)은 직접 가시로 판독 크게 개선. **판단 항목(P1/계약)**: 샷의 `silhouette_dummy` 가 판 뒤 2.5 m 라 계약
  샷에는 실루엣이 맺히지 않는다 — **PATCH-008-B 판정: 더미 이동 반려, 샷 3 을 실내 광원 폐색 구성으로 재정의(R4 이전).**
- **PATCH-008 접수(2026-09-18)**: A 캡처 계속 승인 / B 샷 3 R4 이전 실내 광원 폐색 재정의(더미 이동 반려, 플레이 거리 2.5 m 유지) — 대조 결과 현재 투과
  모델은 태양 전용이라 **P3 점광 투과·폐색 작업이 추가로 필요** → r4wt 에 구현·실측 완료(점광 투과 T_p .6 + 해석적 캡슐 차폐, 실내 등롱 더미 뒤 3.5 m,
  더미 z 판 중심): 플레이 거리 2.5 m·.95 에서 실루엣 대비 .315·경계 12 px, 피격 .15 에서 .48, 프로그램 45 불변 — R3 게이트 종료 후 병합 / C 설계 사실 기록 / D 색상 양자화 트립와이어(>0.5% ∧ 과반 저채도
  저휘도) / E 007-C 전제 오류 기록·밴드 유지·크로마 .8 확정 / F R3→R4 비교 11샷. R4 착수 전 목록 5항(CONTRACT-NOTES).
- **PATCH-009 접수(2026-09-18)**: A 샷 3 재정의 승인 / B `shotaudit.mjs`(샷 감시 대상 기계 검사: 절두체·가시성·최소 면적, 케이스 23) P4 편입 — R4 캡처 전
  12샷 1회 실행 / C `docs/P1-DEBT.md` 개설(4항) / D 창호지 '유리' 인상은 P4C 재평가 / E 측정 기록 확인. P4 브리프는 P3 종료 후 별도.
- **PATCH-010 접수(2026-09-18)**: A/B/C 캐릭터 기하·애니메이션 에셋 허용(P4B 전 적용, R3 분류 무관; 게이트 파급표·삼각형 예산·라이선스 대장 기록) /
  D 실측 플랫폼 = 실제 GPU(플랫폼 무관): r4wt 에 `--gpu`·`--headful` 경로 + 환경 식별 기록, `docs/PROFILE-RUN.md`(Windows 절차) — 실 GPU 테스트는
  R3 게이트 뒤. shotaudit 판단 2건 반영(2% 회귀 가드 바닥, 샷 5 재조준 보류).
- **007-E 부연 미구현**: kit.js 처마는 원형 서까래 한 단 — 부연·평고대 없음, P1 이월. R3 비평의 "처마 얇다"는 먼저 여기.
- **006-G tris_scene 정의 고정**: `getSceneTriangles` byGroup(world/viewmodel/fx/sky/other)·`userData.auditOnly` 제외, profile `trisScene.byGroup`.
  §9 동결 검증 수치는 `world` 성분.
- **R3 미착수 P3 후보 → R4 준비 완료(r4wt, 병합은 R3 게이트 뒤)**: 기둥 결 동일 패턴은 월드 슬라이스 공유가 기전(고역 NCC .78) → 인스턴스 원점 해시
  오프셋으로 .43; 접지 음영은 GTAO 기법 한계(눈높이에서 바닥 옆 기둥 폐색 1.00, 6종 스윕) → 부팅 베이크 접촉 AO 맵 신설(근접/원거리 휘도비 4 기둥 일관
  감소); N5 는 공포 기하 밀도 지배(텍스처 페이드 −3%) → P1 이월; N3 는 샷 계약 조명(스침광 기둥 그림자)으로 기록. 전부 프로그램 45·컴파일 0 유지.
  N1(창호지 확산 투과 모델)·N2(앙토 .30 + 서까래 하면 분기)·회백색 목재(크로마 .8)·지붕 형태 일부(007-A)는 R3 에 반영됐다.

## 6. 정직한 한계
- 모든 성능 절대값은 소프트웨어 렌더러 스케일. 결정성·팔레트·알베도·프로그램 수·컴파일 0은 환경 무관 성질이다.
- 결정성은 SwiftShader에서 입증됐다. 실제 GPU 드라이버는 다른 비결정 경로(예: 병렬 리덕션)를 가질 수 있어 §10
  실측 시 baseline ×2를 실기기에서도 한 번 돌릴 것을 권한다.
- 비평은 브리프 원문 지시문이 아니라 재구성 지시문으로 수행된다(기록).
- C4 그레이드의 대역 채도 상한은 팔레트 규율을 룩이 강제하는 장치다 — 재질·조명 결함을 가릴 수 있으므로 C3에서
  재질(크로마)·조명(야간 하늘광)을 먼저 고치고 LUT는 2차 보호로 뒀다.
- 실시간 모드 시뮬 시간 소화율 지표는 profile 통계에만 있고 게이트가 아니다(C2 기록).

## 6-1. 비평 라운드 R1 결과와 수정 라운드
- R1(baseline/c4, 블라인드 비평자 12세션 순차, 재구성 지시문): 결함 합계 **157**, 미완성 **12/12**, 평균 점수 3.1 →
  **라운드 실패**. 샷별 원문은 `docs/critique/R1-S01..S12-*.md`, 집계·진단은 `R1-SUMMARY.md`.
- 결함 묶음 A–H 분류: **수정**(A 하늘·대기, B 그림자·접지·조명, C 지붕 재질, D 목재·흙·회 재질, E 단청 박리·축척,
  F 창호지, G 파편 조명) / **기록**(샷 태양 방위 = 계약) / **이월**(H 레벨·콘텐츠 → P1·P4, 공포·지붕 형태 → P1 §9,
  총구화염·예광·뷰모델 형태 → P2A/P2B).
- 수정 라운드(`r1-fix`): 값 표와 판정은 CONTRACT-NOTES "R1 판정 기록". 요지 — GTAO 반경 .25→.7 m·반구광 .4→.25·
  등롱 감쇠 완화(B), 안개 .0022→.0012·헤이즈 .3→.2·그레이드 대비 1.16·돔 권운층(A), 보토 셸 전용 재질 + 하면
  서까래 분기·기와 의도 알베도 .07→.13(매니페스트 갱신)(C/G), 목재 결 신장·반점 축소·거시 변조(D), 박리 .35(E),
  창호지 불투명도 .82(F). 전부 유니폼 분기·값 변경이라 프로그램 순열 불변(사전점검에서 확인).
- **R2 결과(baseline/r1, 12세션 순차, 동일 지시문)**: 결함 합계 **131**(R1 157, −26 / −16.6%), 미완성 **12/12**, 평균 3.33(3.08)
  → **라운드 실패**(결함 감소 조건은 충족, 미완성 ≤3 미충족). 샷별·증상별 변화와 새로 생긴 결함(창호지 발광 과다, 앙토 과명,
  실내 그림자 줄무늬 등 P3 소유 5건)은 `docs/critique/R2-SUMMARY.md`. 비평자가 '미완성'의 근거로 든 것은 12/12에서 텅 빈
  지면·직선 지평선·원경 부재·소품/사람/HUD 부재·기본 도형 뷰모델·지붕 형태로, P1·P2·P4 소유다.
- 판정: P3 재질·조명·톤 수정만으로는 미완성 판정을 뒤집을 수 없다는 것이 R1·R2의 일관된 결과다. 브리프 순서대로 보고 후 정지하며,
  R3(P3 측 후보 N1–N5)는 발주자 지시 대기.

## 6-2. R1 수정 라운드 게이트 (baseline/r1, 코드 6a900cf)
- 비트 동일 ×2: **12/12** (첫 패스 18:24–23:27, 검증 23:27–04:22). 전환 imagediff c4→r1: 12/12 상이(전역).
- profile p3: 전부 green(programs 45, 컴파일 0, overdraw 2.001), **부팅 15.5 s(초과)**. harnesstest 18/18. 팔레트 12/12(최악 .168%).
  albedo ok(4.47/5.045, 편차 0 — GTAO 확대가 감사 리그 암카드 폐색을 더해 비율이 기대값에 더 근접). viewmodel .9972, playtest ok.
  원 출력·3항 점검은 CONTRACT-NOTES "R1 스냅샷".

## 6-3. R3 비평 결과 (baseline/r3, 코드 d47fbe5, 2026-09-19) — **재구성본 계측 (PATCH-012-C)**

> 아래 R1·R2·R3 수치는 재구성 지시문으로 잰 것이다. 계약 원문 지시문 기준선은 R3′ 이며 절대값을 섞어 비교하지 않는다.

| 지표 | R1 | R2 | R3 |
|---|---|---|---|
| 결함 합계 (12샷) | 157 | 131 | **116** (−15, −11.5%) |
| 미완성 판정 | 12/12 | 12/12 | 12/12 (기록만) |
| 점수 평균 | 3.08 | 3.33 | 3.42 |
| P3 소유 | — | 62 | 53 |
| world(P1) 소유 | — | 49 | 50 |

- 라운드 판정 **통과(감소)**. R3→R4 비교 기준선은 샷 3 제외 11샷 **106건**(PATCH-008-F).
- 줄어든 것: 팔작지붕 반전 계열("흰 줄무늬 판") 소멸, 창호지 N1(격자 하단 밝은 띠·판이 하늘 위에 떠 있음) 소멸, S08 N3 약화, 하늘 단색 8/12 → 3/12.
- 남은 것: 접지 음영 없음 9/12, N3 실내 그림자 줄무늬 4샷, 재질 반복·동일성(기둥 결·지면 타일·담장), 단청 매핑 불연속, 앙토 N2, 점광(S06 3건).
  전부 r4wt R4 후보 묶음과 겹친다(CONTRACT-NOTES "R4 후보 측정").
- 발주자 판단 없이 진행: 없음. 상세는 `docs/critique/R3-SUMMARY.md`·`R3-OWNERSHIP.md`.

## 6-4. R3′ 원문 기준선 · r4wt 병합 · 011-E 종결 (2026-09-19)

### 비평 도구 교체 (PATCH-012)
계약 번들 수령으로 비평가 지시문 **원문**(P3-BRIEF §2-3, 4항목)을 확보했다. R1–R3 는 원문이 없어 재구성한 5항목 판본으로 측정한 **계약 위반**이었다.
R3 이미지 12 장을 원문 지시문으로 다시 평가해 **R3′** 기준선을 세웠다.

| 지표 | R3′ (원문) | R3 (재구성본) |
|---|---|---|
| 결함 합계 12샷 | **245** | 116 |
| R4 합격 기준선 | **10샷 208** (샷 3·9 제외) | — |
| 미완성 판정 | 12/12 | 12/12 |
| 점수 평균 | 2.62 | 3.42 |

도구 배율은 ×1.58–3.00 으로 **단일 환산계수가 없다** — 전수 재측정이 유일한 길이었다. 판정은 12/12 에서 양쪽이 같아
**PATCH-005-A(미완성 게이트는 P3 범위 밖)는 유지**된다(012-D 표의 "10 이상" 구간). 내가 세웠던 "재구성본 어형이 미완성 쪽으로 편향시킨다"는 가설은 기각됐다.

### R3′ 소유·등급 (245건, 새로 부여)
P3 **146**(render 81·materials 46·sky 19) / world **49** / **계약 28**(순수 카메라 구도 — P3-DEBT 에 넣지 않는다) / P2 19(fx 12·weapons 7) / ui 2 / 불가 1.
등급 A 9 · B 12 · C 8 · D 171 · E 45. **R4 판정: 10샷 208 감소 = 합격, B 등급 11건 감소 여부 별도 보고, A 9건은 정성 보고, 샷 9 는 정성 판정.**

### 011-E 창호지 측정 종결 (PATCH-013-A)
판 23 개 전부 `.95`·DoubleSide·측벽 포함. 주간 샷에서 **알파 비침은 평균 0.2/255·영향 픽셀 2 % 이하**이고, 투과를 끄면 구조 판독성이 25 %(대청)·12 %(샷 3)
떨어진다 — **보이는 것은 종이를 통과한 상이 아니라 종이에 투영된 그림자**이며 실루엣 메커닉이 설계대로 작동한다. 계약 작성자의 원인 후보 셋은 전부 기각.
예외는 `hanji_pierced` 하나로, 피격 누적이 판 전체 불투명도를 하한 `.08` 까지 **균일하게** 떨어뜨려 알파 비침이 판 픽셀의 99.3 % 를 지배한다 —
**게임 규칙 결함**이며 PATCH-013-B 구멍 목록 모델로 교체한다(R4 4 번, P4C 전 필수).

### 가설 5(창살 반투명) 기각과 방향 실측
창살은 불투명하다(숨기면 화면 22.65 % 가 평균 31.2/255 변함). 막대 폭은 756 px 환산 4 px 로 서브픽셀도 아니다. 원인은 **대청 전면 4 베이의 창살 방향 반전**
(창살이 실내 쪽)과 피격 상태의 조합이었다. 전수 대조 결과 나머지 11 판은 정상이다. **P1-DEBT 1 순위에 방향+패턴을 함께 기록**했고, 재발 방지로
`geometryaudit [8]` **톱니 모드**(알려진 위반 4 건 상한, 초과 시 exit 1, 음성 케이스 24)를 신설했다 — 방향 부호 오류 3 번째이고 셋 다 게이트가 아니라 육안으로 찾았다.

### r4wt 병합 (ecec7a8)
샷 3 야간 재정의·점광 투과·해석적 캡슐 차폐, 베이크 접지 AO, 기둥 결 인스턴스 해시, 단청 원경 페이드, `shotaudit`, GPU 기동 경로, 팔레트 양자화 트립와이어,
P5 정적 배포 설정을 main 에 병합했다. 병합 후 점검: geometry·chain·계약 게이트 4 종(fx·surface·cover·npm test)·shotaudit 12/12·팔레트 트립와이어 미발동 전부 통과.

## 7. 다음 단계
- C4 phase-2: profile p3 → harnesstest 18 → palette/albedo/viewmodel/playtest → C4 스냅샷·3항 점검 기록.
- 수정 라운드 사전점검(알베도 gain 역산 확정·팔레트·컴파일 0·rendervariance·프로그램 수) → main 병합 → 계약 캡처 ×2
  (baseline/r1) → 게이트 → R2(12세션 순차) → R2 집계(결함 <157, 미완성 ≤3 여부) → 이 보고 확정 → 정지.

## 8. R4 종료 · P3 종료 보고 (R4-BRIEF §7, 2026-09-25)

**전제**: 마감 게이트 체인 3차 ALL GREEN(스냅샷 `b5abed6`, 18 단계, 캡처 24/24 비트 동일, 프로그램 44) → `baseline/r4` 확정 → 원문 지시문 비평 12 세션 →
분류·판정. 발주자에게 캡처 12 장 전달 완료. **배포 브랜치 `claude/new-session-qfglz6` 를 `r4-wip` 로 fast-forward** 했으므로 Pages 빌드가 R4 커밋을 반영한다.

1. **착수 전 모순 스캔** — 브리프 §5 "완전한 게이트 목록"에서 `rendervariance` 누락(장부 #14, 발주자 승인으로 추가). 착수 뒤 드러난 것: 013-B 가 교체되는 시스템의
   시각 짝(`fx_hanji_tears`)을 열거하지 않음(#25), 미전달 P4-BRIEF §4-6 "산탄은 창살을 뚫지 못한다"(#26). 상세 `docs/CONTRACT-NOTES.md`.
2. **이월 6 건 대응표** — 전부 대응(`docs/CARRYOVER-AUDIT.md`): 뷰모델 재질 → 작업 1(A) · 파티클 색/발광/톤 → 2(B) · 데칼 알베도·노멀 → 2(B) · 예광 색온도 → 3(B) ·
   화염 강도·색온도·감쇠 → 3(B) · 창호지 구멍 시각 산란 → 4(C). 누락 0.
3. **작업 1~6 결과 · 프로그램 수** — 1 뷰모델 건메탈·폴리머 분리 + 알베도 매니페스트(albedoaudit 편차 0, viewmodelaudit 조도비 .990) · 2 프로파일 15 종 색·발광 + 종횡비·
   회전 + 알파 아틀라스 + 표면별 탄흔(fxaudit 시각 2 축 105 쌍) · 3 화염 곡선·색온도·감쇠, 예광 색온도, 화염 알파 모양 · 4 구멍 목록 모델(아래) · **5 미해결**(스펙큘러 AA·이방성
   둘 다 실측 기각, P3-DEBT) · 6 병합분(접지 AO·기둥 결 해시·단청 원경 페이드)은 R4 비평에서 **효과가 읽히지 않았다** — 접지 그림자 부재가 10/12 샷에서 다시 지적됐다.
   프로그램 45 → 46(+1 입자 알파 아틀라스) → 45(찢김 쿼드 제거) → **44**(GTAO 노멀 패스에서 FX 제외). 예산 110.
4. **구멍 모델 실측** — 반지름 `CARBINE .026 / SHOTGUN .014 / DMR .020` m, 임계 **T = 6**(양방향 p = 9, 발주자 승인). 목표 동작: 카빈 한 발 2.5 m 판독 불가 · 군집 시
   판독 가능(대비 .309, 경계 0 px) · 산탄 한 발 3 m 찢어짐(9 펠릿 전부 같은 판) · 6 m 에서는 p_min = 4 로 찢어지지 않을 수 있음(설계, 게임 정보). playtest 검사 6 종 +
   harnesstest 케이스 26 — **케이스 26 이 양성 검사의 공허 통과를 드러내 정정**(1 차 3 발/구멍 3, 2 차 11 발 찢어짐). 상세 `docs/R4-LOG.md`.
5. **10 샷 결함 수 vs 208** — **144 (−64, −30.8 %) → 합격.** 10 샷 전부 감소.
6. **B 등급 vs 11** — **8 → 감소(재편 성공).** S08 총구 연기·예광 2, S12 파편 6.
7. **A 등급 정성(§2-3)** — 세 기준 모두 미달. ① 면 방향 명도: "윗면과 옆면의 명도 차이가 없어 면 구분도 되지 않는다"(S08 #1) ② 접지·AO: 어느 세션도 뷰모델 접지를 언급하지
   않음 ③ 시선: "화면의 1/4 을 차지하는 전경 총기가… 프레임을 지배한다"(S08 판정문). 재질 분리는 "직선 경계로 갈라진 점박이 텍스처"(S08 #2)·"진한 남색… 합성한 것처럼"(S11 #10)
   으로 읽혔다. A 6 건(R3′ 9). 기하는 P4B.
8. **샷 9 정성** — "중앙 평면이 거의 균일한 밝은 회백색"(#1) · "띠들의 상·하단 실루엣이 들쭉날쭉하게 찢긴 형태로 끝나고, 어떤 물체의 윤곽으로도 식별되지 않는다"(#3) ·
   "세 개의 수평 열로 정렬… 격자 배치로 읽힌다"(#4). **유리 아님(해소), 빈 창살 아님, 그러나 찢긴 종이로도 읽히지 않음** — 찢어짐 마스크의 칸 행 규칙성이 무늬가 됐다.
   탄흔 구멍 12 개는 "고립된 검은 점"(#10)으로, 구멍으로 식별되지 않았다(픽셀 소유 실측). 점수 1/10. 등급 C 로 P3-DEBT.
9. **미완성 전환** — 없음, 12/12. B 등급 수정의 역할: 전환된 샷이 없으므로 없음. 비평자가 든 미완성 근거는 여전히 "완성 요소와 플레이스홀더의 공존"이다 — S07 #9 "건물은
   세밀하게 렌더된 반면, 담장은 두 면짜리 박스, 지면은 노이즈 한 겹의 평면", S11 판정문 "한 오브젝트만 마감된 채 주변이 비어 있는 대비 자체가 블록아웃 단계". 즉 A·B 를 줄여도
   world 소유(빈 마당·박스 담장·기준물 부재, 34 건)와 D 등급 조명(그림자·접지 0)이 판정을 쥐고 있다.
10. **P3-DEBT 최종 규모 · P3 → P4 이월** — 잔여 P3 소유 **118 건**(render 68 · materials 43 · sky 7; A 2 · C 14 · D 86 · E 16) + 작업 5 미해결 + FABRIC 스케일 +
    원거리 형체 판독 후보 + 6 m p_min 게임 정보(`docs/P3-DEBT.md`). "100 건 이상" 예상 적중. P3 → P4 이월 표 확정(`docs/CARRYOVER-AUDIT.md`, 12 행).
11. **정직한 한계** — (a) 비평자는 사람이 아니라 독립 컨텍스트의 모델 세션이며 756 px 축소본을 봤다(R3′ 와 동일 조건이라 비교는 성립). (b) 판정을 쥔 것은 P3 가 손대지 않은
    world·조명 D 등급이라 결함 수 감소가 "완성"에 가까워졌다는 뜻은 아니다 — 12/12 미완성 그대로. (c) 작업 5 무아레·파이어플라이 미해결(S04 #1·2, S10 #1 로 다시 지적).
    (d) 실 GPU 실측은 여전히 없다(SwiftShader, gpuDependent 무효). (e) 구멍 상태(임계 미만) 샷이 계약 12 샷에 없어 구멍의 판독성은 프로브로만 쟀다. (f) 체인이 17 h 38 m
    걸린 것은 기계(R3 대비 CPU 2 배 느림)이고, 그 사이 리그 결함 둘(상한·공허 검사)이 내 쪽에서 나왔다. (g) 찢어짐 마스크 규칙성·탄흔 구멍 미식별은 R4 안에서 고치지 않았다(R5 없음).
12. **계약 모호·충돌 신규** — #26(§4-6 산탄 문장, 미전달 초안). 규칙 제안 둘: ① 감사 상한은 **실측 × 2.5** 로 두고 시간 초과를 요약 줄에 명시(HARNESS 반영 완료)
    ② 게이트 검사 명세는 **비공허 조건**을 함께 적는다 — "N 발 → 구멍 N 개"는 N ≥ 1 인 판이 있어야 검사다(014-D 는 이를 요구하지 않았다). 브리프 §6-6 "배포 빌드가 R4 커밋을
    반영하면 알려라"는 fast-forward 로 이행.

### P4 착수 전 필수 (재요청)
**실제 GPU 성능 측정**(`PATCH-005-H`, `010-D`) — `docs/PROFILE-RUN.md` 절차로 발주자 측 실행을 다시 요청한다. 컨테이너에서는 gpuDependent 가 무효다.

**P3 종료를 선언한다. P4-BRIEF 수령 대기.** 수령 시 절차: 즉시 `docs/contracts/briefs/` 커밋 → 모순 스캔 → 이월 표 대조(011-B).

