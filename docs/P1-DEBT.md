# P1 회귀 패스 후보 목록 (PATCH-009-C 개설 2026-09-18, PATCH-011-D 재정렬 2026-09-19)

P1(레벨·지형·킷 형태·소품 배치) 소유 항목이 별도 패스가 필요한 규모에 도달해 개설한다. **여기 항목은 지금 고치지 않는다.**
착수 시점은 **P4 종료 후 판단**한다 — P4 가 액터·가젯으로 월드 요구사항을 바꿀 수 있으므로 그 전에 P1 을 손대면 두 번 한다.
**단 창살(1번)만은 R4 종료 시 P4B 이전 처리 여부를 재판단한다**(011-D) — `silhouetteaudit` 이 창살 뒤 실루엣을 재기 때문이다.

순서는 **화면 노출 등급**(PATCH-011-A) 순이다. 등급은 작업 순서만 정하고 합격선은 바꾸지 않는다.

| 순 | 등급 | 항목 | 현재 상태 (실측) | 출처 |
|---|---|---|---|---|
| 1 | **C** | **창살 패턴 + 창호지·창살 앞뒤 방향** | 감옥 창살로 읽힘. 코드 실측: 세로살 **24 mm 각재**(`kit.js LATTICE_T 0.024`)가 **피치 ≈160 mm**(`level.js` `nV = round(w/0.16) − 1`, 간격 = w/(nV+1) — 베이 폭과 무관하게 160 mm 고정), 가로띠는 **높이 −36 %·0 %·+36 % 3 개뿐**. 띠살·용자살은 세로 피치 60–90 mm + 가로띠 3 묶음(묶음당 3–5 개)이므로 **밀도가 약 2–3 배 부족**하다. 변경 시 `WOOD_LATTICE` 두께·밀도가 바뀌므로 **`chainaudit` 재검증 필수**(관통 체인 24 mm). **[방향 실측 2026-09-19]** 전통 한옥은 창호지를 창살 **안쪽(실내)** 에 붙이므로 실외에서 보면 창살이 앞이어야 한다. `hanjiBay(..., out)` 전수 대조: 내아 서면(`na_w`)·내아 남면(`na_s`)·관아 남면(`gs_s`)·관아 서면(`gs_w`) 4 면은 **정상**(창살이 실외), **대청 전면 4 베이(`dh_bay`, z −19.5, `out −1`)만 반전** — 대청 실내는 z < −19.5(후면 심벽 −28.5)인데 창살을 z −19.516, 즉 **실내 쪽**에 두었다. 대청 칸막이(`dh_part`) 4 개는 양쪽이 다 실내라 전통 기준이 적용되지 않는다(현재는 중앙 통로 쪽에 창살). **이 반전이 샷 9 판독을 바꿨다** — 카메라(z −17.3)가 마당 쪽이라 창살을 **종이 너머로** 보게 된다. 창살 패턴 작업 시 방향을 함께 정한다. 지금 고치지 않는다 | PATCH-011-D, 발주자 열람, 방향 실측 |
| 2 | D | **등롱** | 현대 가로등으로 읽힘(검은 기둥 + 발광 정육면체). 조선 등롱(괘등·석등·좌등) 형태로. R3 S06-1·S08-7 | PATCH-011-D |
| 3 | D | **담장 기와** | 평판 슬래브 — 기와골 없음. R3 S06-5·S07-3·S12-2/7 | PATCH-011-D |
| 4 | D | **부연 미구현** | 홑처마(원형 서까래 한 단, 부연·초매기·평고대 없음). "처마가 얇다"류 지적의 원인 후보. R3 S01-10·S04-9 | PATCH-007-E |
| 5 | D | **공포 기하 밀도** | 원경 지글거림을 텍스처가 아니라 기하가 지배(단청 페이드 −3 %). 기하 LOD 또는 밀도 조정. R3 S09-7(원경에서 색점으로만 읽힘) | N5 재분류 (CONTRACT-NOTES R4 후보 측정) |
| 6 | D | **빈 마당 · 소품 · 원경** | R2 49 건 + R3 50 건(대부분 동일 결함의 재지적 — 합산이 아니다). 빈 지면·직선 지평선·원경 없음(12/12 반복) · 소품 형태/배치 · 기단/계단/주춧돌 · 문짝/문틀 · 담장 단조 · 지붕 두께/기와 골/용마루 · 회랑 끝 막힘 · 마루 단차 | `R2-OWNERSHIP.md` · `R3-OWNERSHIP.md` |
| — | 규칙 | **더미 배치 규칙** | 샷 대상은 불투명체 뒤에 두지 않는다 — `silhouette_dummy` 가 샷 카메라 축의 중앙 기둥 뒤에 있었다(z=cz). `tools/shotaudit.mjs`(PATCH-009-B)가 기계 검사하므로 재발은 게이트가 막는다 | PATCH-009-A |

추가 등재는 출처(패치·비평 라운드·측정 기록)를 반드시 적는다.
