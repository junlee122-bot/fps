# ARCHITECTURE.md

**프로젝트 코드명:** JOSEON-CQB
**전제:** 조선 후기 목조 건축 공간 + 현대 화기. 시대 정합성은 설명하지 않는다.
**핵심 훅:** 종이와 나무로 지어진 공간에서의 현대 총격전. 벽이 엄폐물이 아니다.
**런타임 의존성:** `three` 단독. 아트 에셋 0개. 모든 텍스처·메시·사운드는 로드 타임 절차적 생성.

이 문서는 에이전트가 작업하는 **계약서**다. 서브시스템 인터페이스, 디렉터리 소유권,
크로스 서브시스템 이벤트 어휘, 공유 표면 타입을 정의한다. 이 문서와 충돌하는 구현은
구현이 틀린 것이다.

---

## 0. 비목표 (Non-goals)

명시적으로 추구하지 않는 것. 에이전트는 아래를 개선하려 시도하지 않는다.

- **포토리얼리즘.** 절차적 텍스처는 근거리에서 사진이 아니라 노이즈로 읽힌다.
  참조 레포가 5.05/10에서 멈춘 지점이 정확히 여기다. 목표 룩은 **단청 팔레트 기반
  스타일라이즈드**이며, 이는 타협이 아니라 선택이다.
- **인물 피부·손가락 디테일.** 절차적 생성의 최약점. 적은 중거리 이상에서만 교전하도록
  레벨을 설계하고, 뷰모델은 장갑 착용으로 손가락 문제를 회피한다.
- **실제 GI.** 근사로 간다.
- **콘텐츠 볼륨.** v0는 맵 1개, 무기 3종, 적 1종, 플레이 5분.

---

## 1. 서브시스템 및 디렉터리 소유권

**에이전트 1인당 1디렉터리. 타 디렉터리 파일을 수정할 수 없다.**
크로스 서브시스템 변경이 필요하면 이벤트 어휘(§3)를 통하거나, 계약 변경 제안을 낸다.

| 디렉터리 | 서브시스템 | 참조 레포 재사용도 | 책임 |
|---|---|---|---|
| `src/core` | 부트·클록·프리웜 | **높음** | 엔진 클록(단일 시간 소스), 셰이더 프리웜, 에셋 파이프라인 |
| `src/render` | 렌더 파이프라인 | **높음** | HDR, CSM, MRT prepass, GTAO, TAA, 모션블러, 블룸, EV100 미터링, 그레이드 LUT, AgX |
| `src/materials` | 텍스처 포지 | **낮음 — 전면 재작성** | 조선 표면 14종 절차적 생성. §4 표면 타입 계약 준수 |
| `src/sky` | 대기·조명 환경 | 중간 | 산란, 시간대, PMREM, 볼류메트릭 안개·광선. 톤은 수묵 대기원근 |
| `src/world` | 레벨 | **낮음 — K-TEKTON 이식** | 한옥 모듈 킷, 공포 구조, 기와 곡면, 배치 프롭 |
| `src/physics` | 물리 | **높음** | BVH, swept-capsule 컨트롤러, 임펄스 강체+CCD, PBD 래그돌, **다층 관통** |
| `src/player` | 플레이어 | **높음** | 이동 상태머신, 슬라이드/맨틀/린, 카메라 필 |
| `src/weapons` | 화기 | **높음** | 절차적 무기 지오메트리, 뷰모델 리그, ADS, 스프링 반동, 탄도 |
| `src/fx` | 이펙트 | 중간 | GPU 파티클, 데칼, 예광, 총구화염, 폭발. **파편 계열은 표면 타입 구동** |
| `src/ai` | AI | **높음** | 내비메시, 지각, 엄폐, 래그돌 사망 |
| `src/ui` | HUD | **높음** | DOM/CSS. 크로스헤어, 히트마커, 미니맵, 킬피드 |
| `src/audio` | 오디오 | 중간 | Web Audio 합성. 사운드 파일 0개. HRTF, 컨볼루션 리버브, 오클루전 |

### 결합 관심사 주의

**`render` / `sky` / `materials` / 톤매핑은 하나의 결합 시스템이다.**
참조 레포에서 이 셋을 병렬 에이전트로 나눈 3라운드는 점수 +0.46에 결함이 60→66으로
증가했고, 한 명이 순차로 소유한 1패스가 +1.00에 결함 66→26을 기록했다.

> **규칙: 조명·색·톤매핑 관련 작업은 반드시 단일 오너가 순차 패스로 수행한다.**
> 이 세 디렉터리를 서로 다른 에이전트에 동시 할당하지 않는다.

---

## 2. 표면 타입 계약 (핵심)

**이 프로젝트의 게임플레이는 이 표에서 나온다.** `materials`가 정의하고, `physics`가
관통을 계산하고, `fx`가 파편을 고르고, `audio`가 임팩트 사운드를 고른다. 네 서브시스템이
같은 enum을 참조한다.

`SurfaceType`은 `src/core/surfaces.js`에 정의되며 **어떤 에이전트도 단독 수정 불가**.

> **[PATCH-001-B]** 아래 표의 `energyRetain (파라미터)` 열은 관측되는 잔여 에너지가
> 아니라 `surfaces.js`의 상수 입력값이다. 실제 잔여 에너지는 두께와 밀도가 지수로
> 적용된 결과이며, **유일한 규범적 정의는 `computePenetration()`의 출력이다.**
> 게임플레이 밸런스 논의는 이 표가 아니라 함수 출력을 근거로 한다.

| 표면 | 관통 | energyRetain (파라미터) | 시각 | 임팩트 사운드 | 비고 |
|---|---|---|---|---|---|
| `HANJI` 창호지 | 무제한 | ~99% | 찢김 전파, 구멍 누적, 광투과 변화 | 종이 파열, 매우 약함 | **엄폐물 아님.** 실루엣 사격의 기반 |
| `WOOD_LATTICE` 창살 | 관통 | ~85% | 살대 절단, 파편 | 목재 파쇄 | 창호지와 레이어 결합 |
| `WOOD_PLANK` 마루·문판 | 관통 | ~70% | 스플린터, 관통공 | 목재 저음 | |
| `WOOD_COLUMN` 기둥 | 부분 | ~59% [PATCH-002-D] | 깊은 파임 | 둔탁한 목재 | 소총 관통 가능, 신뢰 불가 엄폐 |
| `EARTH_WALL` 흙벽 | 부분 | ~23.5% [PATCH-002-D] | 흙먼지 분출, 균열 | 먹먹함 | 누적 피격 시 국소 붕괴 |
| `ROOF_TILE` 기와 | 관통 | ~60% | 기와장 파쇄·낙하 | 도자기 파열, 고음 | 낙하물이 물리 객체 |
| `GRANITE` 기단·초석 | 차단 | 0% | 스파크, 석분 | 날카로운 석재 | **수직 건축재 중 유일한 엄폐물** [PATCH-001-A] |
| `DANCHEONG` 단청 도장면 | 하부재 따름 | — | 채색층 박리 → 목재 노출 | 하부재 따름 | 시각 전용 레이어 |
| `LACQUER` 옻칠 | 하부재 따름 | — | 광택면 크레이터 | 하부재 따름 | 시각 전용 레이어 |
| `FABRIC` 무명·삼베·발 | 무제한 | ~98% | 펄럭임, 절단 | 거의 무음 | 시야 차단만 |
| `THATCH` 초가 | 관통 | ~75% | 짚 비산 | 부스럭 | |
| `BRONZE` 범종·솥 | 차단 | 0% | 스파크, 울림 | **장시간 공명** | 오디오 시그니처 |
| `WATER` 연못 | 감쇠 | 거리 기반 | 물기둥 | 물 임팩트 | |
| `PACKED_DIRT` 마당 | 차단 | 0% | 흙먼지 | 둔탁 | |
| `ROOF_SOIL` 지붕 보토 | 관통 | ~39% | 흙 puff (`soil_puff`) | 둔한 흙음 (`soil_dull`) | [PATCH-003-A] 심벽과의 병합 정정. 기와 아래 완충토 |

### 파생 계약

- `HANJI`는 **동적 투과율**을 가진다. 구멍이 늘수록 `opacity`가 감소하고 광투과가 증가한다.
  `materials`가 상태를 소유하고, `render`가 매 프레임 읽는다.
- `physics`의 관통은 **누적 두께 기반 다층 처리**다. 창호지→창살→방 내부→반대편 창호지가
  한 발에 4레이어로 계산된다.
- **[PATCH-001-A]** 완전 차단 표면은 `GRANITE`, `BRONZE`, `PACKED_DIRT` 3종이다.
  이 중 **엄폐물로 기능하는 수직 건축재는 `GRANITE`가 유일하다.**
  `PACKED_DIRT`는 지면이므로 엄폐 후보에서 제외한다.
  `BRONZE`는 의도적 희소 자원이며 맵당 3개 이하로 배치한다.
  **엄폐 가능 지점 = 기단, 초석, 담장 하부, 석등 (+ 희소 BRONZE 프롭).** 그 외 전부 관통된다.
  AI 엄폐 선택과 §5 레벨 규칙 검증은 `VERTICAL_COVER_SURFACES`를 사용하고,
  `COVER_SURFACES`는 물리 질의용으로 유지한다.
- **[PATCH-003-B] 표면 신설 기준 (영구 조항).** 표면을 하나 늘릴 때마다 자유
  파라미터가 하나 늘어난다. 충분히 늘리면 어떤 결과 표든 만족 가능해지고, 그 순간
  표는 제약으로서 의미를 잃는다. 신규 표면은 아래 **3개를 전부** 통과해야 승인된다.
  1. **물리적 구별** — 기존 표면과 재료·밀도·시공이 실제로 다를 것
  2. **독립적 정당화** — 지금 풀려는 수치 문제와 **무관하게** 존재 근거가 설 것.
     "이 표면이 없으면 표가 안 풀린다"는 근거가 되지 않는다
  3. **플레이어 구별 가능성** — `fx`와 `audio` 프로파일이 기존 표면과 다를 것.
     관측 불가능한 표면은 새 재료가 아니라 자유변수다

  3번이 가장 중요하다. 이 조건을 통과하지 못하는 제안은 반려한다.
  (`ROOF_SOIL`은 3개 전부 통과 — PATCH-003-A.)
- **[PATCH-001-D] 표면 매핑에 기본값(fallback)을 두지 않는다.**
  지오메트리→`SurfaceType` 어댑터는 전수 매핑이어야 하며, 미지의 지오메트리에 대해
  조용히 값을 반환하지 말고 **throw한다.** 조용한 게임플레이 결함을 시끄러운 크래시로
  바꾸는 것은 언제나 이득이다. 이 규칙은 `physics`, `ai`, `audio`, `fx` 네 소비자
  전부에 적용된다.

---

## 3. 크로스 서브시스템 이벤트 어휘

단일 이벤트 버스. **아래 목록에 없는 이벤트를 발행하지 않는다.** 신규 이벤트는 계약 변경.

```
weapon:fire          { weaponId, muzzleWorldPos, dir, energy }
weapon:reload:begin  { weaponId, durationMs }
weapon:reload:end    { weaponId }
weapon:ads           { weaponId, active }

ballistic:hit        { surfaceType, worldPos, normal, incidentEnergy, layerIndex }
ballistic:penetrate  { surfaceType, entryPos, exitPos, residualEnergy }
ballistic:stop       { surfaceType, worldPos, normal }

surface:damage       { surfaceId, surfaceType, localUV, radius }
surface:breach       { surfaceId, surfaceType, worldPos }   // 흙벽 국소붕괴, 기와 탈락
surface:opacity      { surfaceId, opacity }                 // HANJI 전용, render 구독

light:transient      { worldPos, intensity, colorK, decayMs } // 총구화염 → render
audio:impact         { surfaceType, worldPos, energy }
audio:occlusion      { listenerPos, sourcePos, surfaceChain[] }

actor:damage         { actorId, amount, hitZone, throughSurfaces[], residualEnergy }  // [PATCH-002-F] 관통은 k가, 피해는 잔여 에너지가 지배
actor:death          { actorId, impulseWorldPos, impulse }

world:tod            { hours }                              // sky 발행
world:weather        { fogDensity, humidity }               // sky 발행
```

**금지:** 서브시스템 간 직접 import. `physics`가 `fx`를 부르지 않는다. 이벤트만 발행한다.

---

## 4. 절차적 머티리얼 목표 (14종)

`materials` 오너의 작업 목록. 전부 주기적 노이즈로 심리스 타일링, Sobel height→normal,
시차 폐색 매핑, 트라이플래너, 곡률 기반 에지 마모를 지원해야 한다.

1. 창호지 (반투과, 섬유 결, 누렇게 변색)
2. 목재 창살 (직교 격자, 결)
3. 소나무 판재 (마루, 나뭇결·옹이)
4. 단청 — **최우선.** 청·적·황·백·흑 5색, 연화·머리초·금문 패턴, 도장층 박리
5. 기와 (곡면, 이끼, 와당 문양)
6. 흙벽 (짚 혼입, 균열)
7. 회벽
8. 화강암 (기단·초석, 정 자국)
9. 옻칠 (고광택, 미세 크레이징)
10. 무명·삼베
11. 초가 (짚단 다발)
12. 청동 (범종, 녹청)
13. 다짐흙 (마당)
14. 종이 등롱 (자발광, 내부 산란)

**팔레트 규율:** 전체 씬은 단청 5색 + 목재/흙 중성색으로 제한한다. 채도 이탈 금지.
이 제약이 절차적 생성의 약점을 감추는 주 수단이다.

---

## 5. 레벨 계약 (v0)

**단일 맵: 관아 또는 사찰 경내, 약 90×90m.**

공간 문법 — 각 공간은 명시적 전술 역할을 가진다.

| 공간 | 전술 역할 |
|---|---|
| 마당 | 개활 킬존. 엄폐 = 석등, 기단뿐 |
| 툇마루 | 측면 이동로. 뒤가 창호지라 안전하지 않음 |
| 대청 | 중앙 홀. 다방향 진입 |
| 방 | 좁은 실내. 벽 4면 전부 관통됨 |
| 회랑 | 순환 동선. 기둥 리듬이 시야 차단 |
| 담장 | 외곽 경계. 하부 화강암(엄폐) / 상부 기와(관통) |
| 누각 | 고지대. 마루 바닥 관통 가능 |

**설계 규칙 [PATCH-001-A 정정]:** 플레이어가 서 있는 어떤 지점에서도,
`VERTICAL_COVER_SURFACES`(GRANITE, 희소 BRONZE) 엄폐물까지의 **경로 거리**가
2초 이동 이내여야 한다. 그렇지 않으면 종이 벽 컨셉이 좌절로 바뀐다.
검증은 `tools/coveraudit.mjs`가 자동화한다.

---

## 6. 하네스 (게임보다 중요)

**처음부터 구축한다. 나중에 붙이면 늦다.** 참조 레포의 최대 자산이 이 부분이다.

| 툴 | 목적 |
|---|---|
| `tools/capture.mjs` | 헤드리스 Chromium(GPU 백엔드)으로 지정 샷 1장 캡처 |
| `tools/shotset.mjs` | 11샷 일괄 — 빠른 리뷰용. **재현성 없음, 게이트로 쓰지 말 것** |
| `tools/baseline.mjs` | **재현 가능 캡처.** 샷마다 격리된 페이지, 고정 프레임 예산. 비트 동일 |
| `tools/imagediff.mjs` | 픽셀 게이트. 1픽셀이라도 움직이면 non-zero exit |
| `tools/profile.mjs` | 실제 DPR 게임플레이 프로파일. p50/p95/p99 + 프레임당 WebGL 프로그램 카운트로 히치 귀속 |
| `tools/playtest.mjs` | 스크립트 이동·사격 스모크 테스트 |

### 반드시 지킬 두 가지

1. **중앙값 프레임타임을 신뢰하지 않는다.** 정적 카메라 벤치가 94fps를 보고하는 동안
   실제 플레이는 12~17fps에 728~1236ms 스톨이 있었다. 원인은 셰이더 34개의 프레임 중
   지연 컴파일. **`src/core/prewarm.js`를 v0부터 넣는다.**
2. **페이지 재사용 캡처는 재현되지 않는다.** 파티클 수명, 데칼 버퍼, 노출 상태가
   앞으로 샌다. `baseline.mjs`는 샷마다 새 페이지를 연다.

### 베이스라인 샷 11종

조선판 회귀 감시 대상. 각 샷은 특정 서브시스템의 실패를 드러내도록 선정한다.

1. `courtyard_noon` — 마당 정오, 강한 직사광 (노출·그림자)
2. `daecheong_backlit` — 대청 역광, 실내에서 마당 (EV 적응)
3. `hanji_silhouette` — 창호지 너머 인물 실루엣 (반투과)
4. `dancheong_closeup` — 처마 단청 근접 (머티리얼 최악 케이스)
5. `roofline_distant` — 기와지붕 원경 (LOD·대기원근)
6. `lantern_night` — 야간 등롱 (자발광·블룸)
7. `fog_wall` — 안개 담장 (볼류메트릭)
8. `muzzle_interior` — 실내 총구화염 (트랜지언트 라이트)
9. `hanji_pierced` — 피격 누적된 창호지 (동적 투과율)
10. `corridor_columns` — 회랑 기둥 리듬 (그림자 캐스케이드 이음매)
11. `viewmodel_ads` — 뷰모델 ADS (뷰모델 조명 리그)

> **알려진 함정:** 참조 레포는 뷰모델 광원 리그가 월드 대비 알베도당 약 20배 조도를
> 전달해, 모든 무기 알베도를 물리값의 1/3로 속여야 했고 이게 가장 오래 보는 물체의
> 재질 분리를 망쳤다. **11번 샷은 v0부터 감시하고, 뷰모델 조명은 월드와 같은 단위로 맞춘다.**

---

## 7. 성능 목표 (v0)

Apple Silicon 노트북, 1512×982, DPR 2, `ultra` 프리셋, AI·사격 활성 상태 기준.

| 지표 | 목표 |
|---|---|
| fps p50 | ≥ 45 |
| fps p99 | ≥ 25 |
| 최악 프레임 | ≤ 50ms |
| 플레이 중 셰이더 컴파일 | **0** |
| 부팅 | ≤ 4s |

참조 레포보다 목표가 높은 이유: 스타일라이즈드 룩이라 지오메트리·셰이딩 비용이
근본적으로 낮아야 정상이다. 삼각형 예산 **6M 상한**, 초과 시 아트 패스를 되돌린다.

---

## 8. 작업 순서

병렬 팬아웃이 아니라 **순차 단일 오너 패스**로 진행한다.

```
P0  core + physics + player          — 이동과 충돌만. 회색 박스 월드
P1  world (K-TEKTON 이식)            — 한옥 모듈 킷, 회색 머티리얼
P2  weapons + fx + physics 관통      — 표면 타입 계약 전체 배선
P3  materials + sky + render         — 단일 오너 순차. 결합 시스템
P4  ai + audio                       — 적 1종, 합성 사운드
P5  ui + 최적화                      — imagediff 픽셀 중립 강제
```

**P3는 절대 분할하지 않는다.** 이 문서에서 가장 중요한 한 줄이다.

각 패스 종료 시 `baseline.mjs` 갱신 → `imagediff.mjs` 통과 → `profile.mjs` p99 기록.
세 개가 다 통과하지 않으면 다음 패스로 넘어가지 않는다.

---

*제작·이승준*
