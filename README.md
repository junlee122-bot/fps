# JOSEON-CQB — P2A 보고 (P1.5·P1·P0 보고는 하단에 보존)

P2A-BRIEF §9 형식. 수치는 게이트 출력(`tmp/gate/p2a_final_*.json`)과
`npm test` 실행 출력 기준. 중간에 계약 블로커 2건이 있었고
(P2A-BLOCKER-001·002 — 둘 다 계약 결함으로 판정되어 PATCH-002·003으로 정정),
본 보고는 정정된 계약(§2-1 정정표 + E 여유 밴드 + ROOF_SOIL)의 이행 결과다.

## 1. 선결 3건

| 항목 | 결과 |
|---|---|
| harnesstest 케이스 9 | chainaudit 음성 훅(--test-drop/--test-reverse) — 둘 다 exit 1 + testOverride 표식. ROOF_SOIL 재태깅 후 재검증 |
| 픽셀 오염 경로 | **없음** (텍스트 렌더 0건 · 캔버스 외 DOM 0건 · SHA/타임스탬프/버전 문자열의 씬 유입 0건 · title 뷰포트 밖 · 텍스처 생성기 부재) |
| §0-3 인스턴스 분할 | **처리** — finalizeInstancing을 18m 그리드 버킷 분할 + 버킷별 인스턴스 바운딩 스피어. tris_frame_p95 133,240 → **121,576** (컬링 작동 개시), 드로우콜 276/900. 부작용: 등심도 이음매의 결정적 미세 픽셀 차이 7샷 ≤71px(≤0.005%) — baseline 갱신에 포함 |

## 2. §2-1 결과 표 실측 매트릭스 — 30칸 전부 통과

분류는 동결 `computePenetration()` 출력만 사용(래퍼는 두께/k 스케일만).
E 밴드: 관통 R≥18.75% / 부분 2.5~12% / 정지 R≤1.6% (BLOCK 제외).
`npm test`(test/margins.test.mjs)가 30칸 분류+여유를 영구 게이트로 고정.

| 표면 (기준 두께) | 카빈 5.56 | 산탄 12ga(펠릿) | DMR 7.62 |
|---|---|---|---|
| HANJI 0.3mm | 관통 99.995% | 관통 99.928% | 관통 99.997% |
| FABRIC 2mm | 관통 99.926% | 관통 99.026% | 관통 99.955% |
| LATTICE 24mm | 관통 91.03% | 관통 28.97% | 관통 94.51% |
| THATCH 200mm | 관통 27.69% | **정지** 0.000% | 관통 46.27% |
| PLANK 45mm | 관통 65.06% | 정지 0.347% | 관통 77.26% |
| TILE 30mm | 관통 65.43% | 정지 0.374% | 관통 77.53% |
| TILE 30+보토 80 | **부분 8.70%** | 정지 0.374% | **관통 23.10%** |
| COLUMN ø300 | **정지 0.943%** | 정지 ~0% | **부분 6.09%** |
| EARTH_WALL 100mm | **정지 0.952%** | 정지 ~0% | **부분 6.12%** |
| GRANITE/BRONZE/PACKED_DIRT | 정지 | 정지 | 정지 |

최소 여유 42.0%(카빈×TILE+보토) — 전 칸 25% 이상, 경계 밀착 없음.
산탄×THATCH '정지'는 PATCH-002-B 정정 반영.

## 3. 최종 파라미터와 튜닝 경위

| 무기 | k (관통 계수) | 초구 에너지 | 탄자 |
|---|---|---|---|
| 카빈 5.56 | **5.6** | 1,800 J | 1 |
| 산탄 12ga | **0.425** | 200 J | 9 펠릿 독립 |
| DMR 7.62 | **9.332** | 3,400 J | 1 |

경위: 에너지 튜닝은 수학적으로 공허(분류가 에너지 불변 — BLOCKER-001 §1 증명)
→ PATCH-002가 k 도입 + retain 2개 해동 + E 밴드 신설 → 5지렛대로도 공집합
(보토가 EARTH_WALL 상수 공유, 비율 상한 1.25 < 필요 1.2664 — BLOCKER-002 증명)
→ PATCH-003이 ROOF_SOIL 분리 승인. 밀도<0.8 제약으로 재도출한 최종값:
retain WOOD_COLUMN 0.59 / EARTH_WALL 0.235 / ROOF_SOIL 0.39 (density 0.50).
에너지는 분류에 무관하며 **피해 전용**(PATCH-002-F) — 동일 분류에서 무기별
잔여 에너지가 다름을 유닛 테스트(§4-2-10)로 고정.

## 4. viewmodelaudit 실측

| 측정 | 값 |
|---|---|
| 월드 카드 (0.18 그레이) 선형 휘도 | 0.10946 |
| 뷰모델 카드 (동일 알베도, 카메라 자식) | 0.10946 |
| **비율** | **1.0000** (허용 1.0±0.10) |
| 검정 카드(알베도 0) | 0.00802 < 배경 0.3028 ✓ |
| 음성 훅 (--test-boost 2) | 비율 1.9389 → exit 1 + testOverride ✓ (harnesstest 케이스 10) |

비율이 정확히 1.0인 것은 우연이 아니라 구조다: 뷰모델은 카메라 자식으로
**월드 씬 안에서** 렌더되므로 별도 광원·노출·알베도 보정이 존재할 자리가 없다.
참조 레포의 20× 조도 사고는 이 리그에서 재현 불가능하다. P3는 이 리그를 상속하라.

## 5. 레이어 체인 4종 성립 증거 (지오메트리 실측 두께)

`collectRayChain` 실측 (진입/출구 쌍 — 상수 두께 없음):

| 체인 | 실측 |
|---|---|
| 창호 다층 | HANJI 0.03cm → LATTICE 2.22cm(살대 모서리 클립) → HANJI 0.07cm(사각 입사 증가) — 한 발에 창호지 2겹 |
| 지붕 관통 | ROOF_TILE 3.25cm → ROOF_SOIL 8.65cm → 서까래(WOOD_COLUMN) 15.16cm |
| 담장 | 상부: ROOF_TILE만(GRANITE 없음, 관통) / 하부: GRANITE 즉시 차단 |
| 누각 마루 | WOOD_PLANK 4.00cm 관통 → 아래층 |

DECAL 포함·스킵과 미매핑 throw(PATCH-001-D)는 BVH 등록 단계에서 강제.
사각 입사 √2배 두께·결구 겹침 합집합은 test/raychain.test.mjs가 고정.

## 6. hanji_pierced 결정성

setShot 시 실제 발사 파이프라인으로 결정적 사격(카빈 3발 + 산탄 1격발 = 12탄자,
시드 스트림 산포). 해당 판 opacity 0.62 → 0.08.
**계약 조건 baseline 2회 실행: 11샷 전부 바이트 동일** (tolerance 0, identical=true) —
사격·산포·HANJI 상태 경신이 전부 결정적임의 직접 증거. resetState가 무기·탄약·
계수·HANJI 상태를 완전 초기화함은 playtest 7e가 검증.

## 7. 선행지표 (GPU-INVALID 환경 — 절대 fps 무효)

| 지표 | 실측 | 예산 |
|---|---|---|
| tris_scene | 134,730 (비가시 콜라이더·마커 468 포함) | ≤ 600,000 |
| tris_frame_p95 | **121,576** (11샷 순회 — §0-3 이전 133,240) | ≤ 250,000 |
| 드로우콜 | 276 | ≤ 900 |
| 프로그램 | 6 (뷰모델·카드·마커가 기존 순열 재사용) | ≤ 40 |
| CPU 프레임 p95 (sim) | 0.6ms (사격 포함 시나리오) | ≤ 6ms |
| 플레이 중 컴파일 | 0 | 0 |
| 부팅 | 462ms | — |

게이트 전체: npm test 17/17 · playtest 44/44(사격 시나리오 13검사 포함) ·
surfaceaudit 미매핑 0(메시 448) · coveraudit 0.12% · chainaudit 9/9 ·
harnesstest 10/10 · viewmodelaudit exit 0 · baseline ×2 비트동일.
baseline 갱신 정당화: hanji_pierced 75.3%(기능 자체) / viewmodel_ads 7.0%(뷰모델
표시) / 7샷 ≤0.005%(§0-3 이음매, 결정적) / lantern_night·muzzle_interior 불변.

## 8. 정직한 한계

1. **카메라 반동 없음** — 반동은 뷰모델 킥·후퇴로만 표현. 조준점 반동은
   스크립트 결정성과 충돌해 보류 (P2B/P4에서 결정 필요).
2. **탄도는 정적 월드 전용** — 강체(상자 3개)는 BVH 밖이라 피격 불가. P2B 과제.
3. **actor:damage 미발행** — 액터가 P4 소유라 대상 부재. residualEnergy 계약은
   어휘·유닛테스트로 고정 (배선 지점은 firecontrol.tracePellet).
4. **HANJI 시각은 균일 불투명도만** — 구멍별 국소 산란은 P3 소유. 구멍 위치
   데이터(판 로컬 UV)는 hanji.js가 저장 중.
5. **muzzle_interior에 뷰모델 미표시** — 샷 의도(총구화염 감시)가 P2B 소관이라
   viewmodel_ads만 표시. P2B에서 재조준과 함께 결정.
6. **viewmodelaudit 음성 훅은 카드 알베도 부스트** — three.js 광원은 씬 전역이라
   '뷰모델 전용 광원'이 구조적으로 불가능(그것이 §3 리그의 보증)하므로, 훅은
   측정·판정 경로가 휘도 편차를 잡는지를 검증한다.
7. **Apple Silicon 실측 미기록** — 계약대로 사용자 몫. GPU-INVALID 환경에서
   절대 성능 판정은 전부 무효.
8. **§0-3 이음매 픽셀** — 버킷 분할로 등심도 이음매 7샷 ≤71px 결정적 변화.
   비트동일 ×2로 결정성은 유지되며, 새 baseline이 기준이 된다.

## 9. 계약 모호·충돌 신규 발견분 (CONTRACT-NOTES 기록)

- **P2A-1**: FABRIC 기준 두께가 두께표에 없음 — 참조값 2mm로 고정 (블로커 보고
  후 무정정 → 채택).
- **P2A-2**: PATCH-001-B 케이스 3 원문("역순 잔여값 다름")은 가환 곱이라 수학적
  불성립 — 실재 관측치(path 순서·stoppedAt 귀속)로 검증하도록 판정.
- **P2A-3**: weapon:fire는 격발당 1건(산탄 총구화염 9개 방지). muzzleWorldPos는
  눈 위치(논리 총구).
- **P2A-4**: §2-1 표는 법선 입사 기준 두께의 규범, 런타임은 레이 실측 두께
  (사각 입사 증가·결구 합집합) — E 밴드 25%가 각도 변동의 완충.

**P2A 종료. P2B(FX)는 별도 지시 대기.**

---

# JOSEON-CQB — P1.5 보고 (P1·P0 보고는 하단에 보존)

P1.5-BRIEF §7 형식. 수치는 게이트 출력(`tmp/gate/p15_*.json`) 기준.

## 1. 삼각형 지표 분리 실측 (§0 계약 정정 이행)

| 지표 | 정의 | 실측 | P1.5 예산 |
|---|---|---|---|
| `tris_scene` | 씬그래프 순회, 인스턴스 전개, 컬링 무관 | **134,218** (비가시 콜라이더 552 포함) | ≤ 600,000 |
| `tris_frame_p95` | 프레임당 가시 삼각형 p95, **11샷 순회**(110샘플) | **133,240** | ≤ 250,000 |

그 외 선행지표: 드로우콜 266/900 · 프로그램 6/40 · CPU p95 0.5ms/6 ·
플레이 중 컴파일 0 · 부팅 451ms. 두 삼각형 수치가 근접한 이유: 소형 경내(90×90)라
광각 샷에서 씬 대부분이 프러스텀 안에 든다 — P3 이후 격차가 벌어질 때 이 분리가 값을 한다.

## 2. 공포 부재 목록과 건물별 출목 구성

부재 6종 전부 개별 지오메트리, 소로는 개별 인스턴스 배치(전 맵 496개):

| 부재 | 처리 |
|---|---|
| 주두 | 4각 lathe — 굽 오목 곡면 프로파일 5점 |
| 소로 | 주두 동형 축소(0.15m). **개별 메시(인스턴스) — 뭉뚱그리지 않음** |
| 첨차 | 압출 프로파일 — 하단 양끝 연화두형 오목 3분절 |
| 살미 | 압출 프로파일 — 쇠서 돌출(끝단 상향 곡선) |
| 행공첨차 | 다포 전용 축소 첨차, 출목선 배치 |
| 장혀·도리 | 공포 상단 결구 라인 — 주심도리 + 최외곽 출목선 외목도리(ø150) |

출목 구성 (실루엣 대비): **객사 = 주심포 1출목**(기둥 위만, 주간포 없음) /
**내아 = 다포 2출목**(주간포 실배치) / **동헌 = 다포 3출목**(주간포 실배치).
전 부재 `WOOD_COLUMN`, 두께표 준수.

## 3. 팔작 2동 (동헌·객사) 처리 방식

- **파라메트릭 두께 셸**: 지붕면을 (u,v) 그리드 함수로 정의하고 노멀 방향 오프셋으로
  닫힌 볼륨 생성 — **기와 30mm + 보토 80mm 2레이어가 곡면·합각하부면에서 실두께로 유지**
  (`chainaudit` 9경로 검증: 주경사면·합각하부면 모두 기와→보토→서까래 순서 성립)
- **처마 곡선**: 앙곡(모서리 상승 0.34, |u|^2.4 가중) + 안허리(평면 내밈 0.30) —
  직선 처마 없음
- **합각·추녀·사래·선자연**: 합각 판벽(45mm 삼각) + 내림마루·추녀마루·용마루,
  모서리 대각 추녀(0.22×0.24)와 사래, 모서리당 6개 부챗살 선자연
- **평면 규칙 결함을 chainaudit이 잡았다**: 초기 구현은 x 반폭이 용마루까지 계속
  좁아져 합각하부면 상단과 합각 벽 사이 1.4m 구멍이 남았고(객사에서 레이 통과),
  "합각 밑선까지만 좁아지고 위는 고정" 규칙으로 수정 후 9/9 통과
- 나머지 3동(내아·행랑·누각)은 맞배 유지 — 대비가 목적

## 4. harnesstest 8케이스 결과 — 전부 통과

1. imagediff 1픽셀 변경 → exit 1 ✓  2. 무값 `--tolerance` → exit 2 거부 ✓
3. profile 무인자 = 계약(30s/3runs/DPR2) ✓  4. 축소 인자 4종 전부 NON-CONTRACT 배너 ✓
5. baseline 2회 바이트 동일 + 비계약 표식 ✓  6. surfaceaudit 미매핑 주입 검출 ✓
7. coveraudit 임계 조작 → exit 1 + 오버라이드 표식 ✓  8. p95·프레임별 합·다중 run 집계 정답 일치(6검증) ✓

이후 모든 패스 종료 조건에 영구 포함.

## 5. 재조준 샷 2종

- `dancheong_closeup`: 다포 3출목 공포대가 프레임 60%+ — 첨차 연화두·살미 쇠서·개별
  소로·장혀/도리 결구가 주 피사체. 그림자 속 저대비는 의도된 머티리얼 최악 케이스
  조건(P3의 숙제)
- `roofline_distant`: 남동 담장 상공 — 팔작 2동(날개 실루엣)과 맞배(내아)·초가(행랑)
  대비가 한 프레임. 누각·회랑은 프레임 밖/모서리 (사유: CONTRACT-NOTES P1.5-5)

## 6. 정직한 한계 (P1.5)

- **공포는 여전히 약식 결구다.** 부재 6종의 형태 언어(굽·연화두·쇠서)는 갖췄지만
  첨차-살미의 십자 맞춤이 실제 사개맞춤이 아니라 관통 교차이고, 출목별 첨차 길이
  변화(2제공·3제공)를 단일 길이로 통일했다. 근접 샷에서 단청을 얹으면 읽히는 수준이나
  건축 도판 수준은 아니다.
- **선자연의 부챗살 각도는 근사다.** 실제 선자연은 추녀 쪽으로 갈수록 서까래 뒤끝이
  한 점에 모이는데, 현재는 야우만 회전시켜 앞끝 간격이 일정하지 않다.
- **합각하부면 기와 인스턴스의 유(u) 방향 뒤틀림이 없다** — 팔작 측면 기와가 경사
  방향으로 일자 배열이라 근경에서 어색할 수 있다 (원경 판별력은 충분).
- **tris_frame_p95(133k)가 tris_scene(134k)에 근접** — 컬링 여유가 없는 소형 씬이라
  이번 패스에서는 두 게이트가 사실상 같은 것을 쟀다. 분리의 가치는 P3부터.
- **Apple Silicon fps 실측은 미기록** — 게이트 아님(§5 이월표). 사용자 실측 대기.
  기록되면 이 절에 추가할 것.

## 7. 계약 모호·충돌 신규 발견분

CONTRACT-NOTES **P1.5-1 ~ P1.5-5**: 해석 승인 기록(재논의 종결) / "조명 변경 금지"와
샷별 태양각의 경계 / tris_scene의 비가시 콜라이더 포함(분리 보고) / 계약 툴 자기검증
훅의 허용 조건(출력 표식 강제) / roofline_distant "맞배 3동" 프레이밍 절충.

**P1.5 종료 게이트 전부 통과** — harnesstest 8/8 · surfaceaudit 393메시 0건 ·
coveraudit 0.12%(레이아웃 보존) · chainaudit 9/9(팔작 곡면·합각 관통 체인 성립) ·
baseline 11샷 2회 비트 동일 · profile 선행지표 5/5 · playtest 통과.
P1.5 종료 후 멈춘다. **P2는 별도 지시를 받는다.**

---

# (보존) P1 보고

P1-BRIEF §7 형식. 모든 수치는 게이트 출력(`tmp/gate/p1_*.json`, 로컬 생성물)에서 왔다.

## 1. 선결 3건 결과

| 선결 | 결과 |
|---|---|
| 0-1 CONTRACT-PATCH-001 | 4패치 적용(surfaces.js는 패치 명시 승인의 신규 export 2건만 추가, 기존 항목 불변 — A/B/C/D 정정문은 ARCHITECTURE·HARNESS 반영). 적용 후 baseline 재캡처 → **11샷 비트 동일 (identical:true)** |
| 0-2 감사 A(결정성) | **순차 단독 실행.** 발견 5건 전부 수정: 런타임 강체 리셋 구멍(A1)·stepFrames 재진입(A2)·컨트롤러 잔여 필드(A3)·이벤트 구독 누적(A4)·settle=0 오캡처(A5). 클린 판정 11개 영역 문서화. 수정 후 playtest 재통과 |
| 0-2 감사 B(하네스 수치) | **A 완료 후 순차 단독 실행.** 발견 9건 전부 수정: NON-CONTRACT 조건 누락(B1 해상도/B6 warmup)·값 없는 tolerance 재발(B2, 실행 입증)·p95 합산 오류(B3)·imagediff 공허 통과(B4)·baseline 무표식(B5)·워밍업 경계 오프바이원(B7)·알파 무시(B8)·shotset 표식(B9). 수정 후 실행 재검증 |
| 0-3 선행지표 게이트 | `profile.mjs` 개편 — 환경 무관 선행지표 4종(예산 게이트, 초과 시 exit 1) / GPU 의존 수치 분리 + `GPU-INVALID` 배너 / `NON-CONTRACT MEASUREMENT` 배너. **발견:** 소프트웨어 GL에서는 `render()` 호출이 라스터에 블록되어(실측 submit p95 263ms) "CPU 시간(GPU 제외)"이 오염됨 → CPU를 sim/submit 2성분으로 분리, 이 환경은 sim만 게이트하고 실 GPU에서는 프레임별 합의 참 p95로 게이트 (`basis` 필드로 공시) |

## 2. 모듈 킷 부재 목록 — 브리핑 §2 표와 대조

`src/world/kit.js`의 `T` 상수가 표와 1:1이다. 전 부재 명명 상수, 매직 넘버 없음.

| 부재 | 표면 | 브리핑 치수 | 구현 |
|---|---|---|---|
| 기단 | GRANITE | h600 / t900 | `KIDAN_H 0.6` / `KIDAN_EDGE 0.9` — 동헌 20×12, 내아 12×8, 객사 14×9 + 3단 계단(단높이 0.2) |
| 초석 | GRANITE | ø600 | `CHOSEOK_D 0.6` 인스턴스 ×75 |
| 기둥 | WOOD_COLUMN | ø300 배흘림 | `COL_D 0.3` LatheGeometry 배 +5% — 높이별 인스턴스(2.9/3.1/3.3/5.6) ×75 |
| 창방·평방 | WOOD_COLUMN | 180×300 | `BEAM_W 0.18 / BEAM_H 0.3` |
| 공포 | WOOD_COLUMN | 주심포 1동/다포 2동 | 객사=주심포 ×12, 동헌·내아=다포 ×38 (병합 지오메트리 인스턴스) |
| 도리·서까래 | WOOD_COLUMN | ø150 | `DORI_D 0.15` 서까래 인스턴스 ×298 (충돌 포함 — 관통 체인 3층) |
| 지붕 기와 | ROOF_TILE+EARTH_WALL | 30 + 보토 80, 2레이어 | 충돌: 곡면 슬랩 `TILE_T 0.03` + `BOTO_T 0.08` / 시각: 수키와 인스턴스 ×6,756 (비충돌 — CONTRACT-NOTES P1-2) |
| 창호지 | HANJI | 0.3mm | `HANJI_T 0.0003` — 시각 평면 + 비가시 0.3mm 충돌 박스 분리 (진입/출구 쌍 보존) |
| 창살 | WOOD_LATTICE | 24mm | `LATTICE_T 0.024` 인스턴스 ×385, 창호지와 별개 레이어 |
| 심벽 | EARTH_WALL | 100mm | `SIMBYEOK_T 0.10` |
| 판벽·판문 | WOOD_PLANK | 45mm | `PANBYEOK_T 0.045` (박공 포함) |
| 우물마루 | WOOD_PLANK | 40mm | `MARU_T 0.040` — 누각 데크 아래→위 관통 가능 |
| 담장 하부/상부 | GRANITE/ROOF_TILE | 900 / 30 | 몸체 h1.7 t0.9 + 기와 갓 30mm (CONTRACT-NOTES P1-5) |
| 마당 | PACKED_DIRT | — | 지면 |
| 범종 | BRONZE | ≤3 | **1개** (surfaceaudit가 상한 검증) |

관통 레이어 체인(P2 테스트 대상) 성립: 창호지(0.3)→창살(24)→[실내]→창살(24)→창호지(0.3) ✓ /
기와(30)→보토(80)→서까래(150)→[실내] ✓ / 담장 상부 기와(관통)·하부 화강암(차단) ✓

## 3. 선행지표 4종 실측 vs 예산 (`node tools/profile.mjs`, 계약 기본값)

| 선행지표 | 실측 | P1 예산 | 판정 |
|---|---|---|---|
| 삼각형 | **112,552** | ≤ 2,000,000 | 통과 (5.6%) |
| 드로우콜 | **224** | ≤ 900 | 통과 |
| 고유 셰이더 프로그램 | **6** | ≤ 40 | 통과 |
| CPU 프레임 p95 (GPU 제외, sim 성분) | **0.7 ms** | ≤ 6 ms | 통과 |

부가: 플레이 중 셰이더 컴파일 **0** (전 기록 구간 판정 — B6 수정 반영), 부팅 중앙값 **441ms**,
BVH 33,164 tris / 97ms 빌드. GPU 의존 수치는 `GPU-INVALID` 배너와 함께 분리 보고
(SwiftShader p50 1.3fps — 판정 무효, Apple Silicon 실측은 사용자 몫).

## 4. coveraudit — §5 2초 규칙

- **위반 0.12%** (도달 가능 6,954셀 중 8) ≤ 기준 2% → **통과**. 히트맵: `tmp/gate/p1_coveraudit.png`
- 잔여 8셀은 13.4~13.8m 경계(객사 동측 실내 모서리·회랑 남단) — 기준 내이므로 유지
- **이 툴이 실제 레벨 결함을 수정시켰다:** 초기 실측 위반 10.15% → 남측 마당이 화강암
  공백임을 히트맵이 드러냄 → 연못 테두리 0.85m 상향, 석등 2기·우물·하마비·부뚜막 추가
  → 3.49% → 프로브 기하 보정(정료대가 8방향 각도 사이로 빠지는 것 실측) → **0.12%**
- 판정 정의(도달 가능성·프로브 기하·초석 제외)는 CONTRACT-NOTES P1-3/P1-4에 명문화

## 5. 정직한 한계 (P1)

- **다포/주심포는 간이형이다.** 첨차·살미 2단 스택 + 소로 수준 — 실제 공포의 출목
  체계(살미 첨차 교차·앙서·초각)와는 거리가 있다. P3 근접 샷(dancheong_closeup)에서
  형태 밀도가 부족하면 P3에서 형태 보강이 아니라 **이 킷의 공포 지오메트리를 보강**해야
  한다 (P3는 색 소유, 형태는 P1 소유물이라 되돌아와야 함).
- **팔작지붕이 없다.** 전 건물 맞배(박공) — 합각·추녀마루가 없어 동헌 위계가 실루엣으로
  덜 읽힌다. 삼각형 예산은 5.6%만 썼으므로 여유는 충분하나 P1 범위 판단으로 미룸.
- **기와 시각 밀도는 수키와 골만이다.** 암키와 골·처마 막새 없음 — 원경에서는 읽히나
  근경(4번 샷)에서 골 사이가 평면으로 보인다. P3에서 노멀/텍스처가 아니라 지오메트리
  불만이 나오면 P1 소유로 회귀할 것.
- **coveraudit의 통행 그래프는 근사다.** 셀 중심 간 2높이 레이(0.45/1.35)로 통행을
  판정하므로 실제 캡슐 스윕과 미세하게 다를 수 있다 — 문 개구 폭 판정이 ±0.5셀 거칠다.
- **행랑 실내 위반 해소용 부뚜막은 사후 배치다** — 히트맵이 시킨 수정이지 처음부터
  설계된 엄폐 리듬이 아니다. P4 AI 엄폐 선택에서 실전 검증될 때 재평가.
- **profile 스크립트는 여전히 이동만 재생한다** (사격 P2, AI P4). 선행지표 게이트는
  유효하나 "실플레이" 명제는 P2부터 점진 충족.

## 6. 계약 모호·충돌 신규 발견분

CONTRACT-NOTES P1-1 ~ P1-6 (회색 규율 vs P0 확립 물성 / 기와 시각·충돌 분리 /
초석 '엄폐 가능' 지위 / coveraudit 판정 구체화 / 담장 구성 미명세 / 부속동 치수 미명세).
전부 채택 해석과 사유가 기록되어 있다.

**P1 종료 게이트 전부 통과** — surfaceaudit 0건(344메시) / coveraudit 0.12% /
baseline 11샷 2회 비트 동일(112k tris 월드에서 결정성 유지) / imagediff exit 0 /
profile 선행지표 4/4 / playtest 통과(P0 앵커 보존으로 무수정).
§9 원칙대로 여기서 멈춘다. **P2는 별도 지시를 받는다.**

---

# (보존) P0 보고

조선 후기 목조 건축 공간의 현대 화기 총격전. **벽이 엄폐물이 아니다** —
창호지·창살·판벽·흙벽·기와가 각기 다른 관통 등급을 가지며 화강암만이 완전 차단재다.

이 문서는 마스터 프롬프트 §8 형식의 **P0 종료 보고**다. 모든 수치는 계측 출력에서 왔다
(`tmp/gate/*.json`은 로컬 생성물 — 재현 명령을 각 절에 병기).

- 계약: `ARCHITECTURE.md` / `HARNESS.md` / `src/core/surfaces.js`(동결)
- 계약 모호·충돌 열거와 채택 해석: `docs/CONTRACT-NOTES.md` (착수 전 작성, 감사 후 2건 증보)

```bash
npm install
npm run serve          # 브라우저에서 접속. 클릭 → 포인터록, WASD/Shift/Space/Ctrl
node tools/playtest.mjs
node tools/baseline.mjs --out baseline --dpr 2
```

---

## 1. 무엇이 들어있는가

| 서브시스템 | 내용 (P0 범위) |
|---|---|
| `src/core` | 단일 시간 소스 `clock.js`(fixed/realtime 모드), 시드 RNG(이름 붙은 스트림), 이벤트 어휘 버스(어휘 밖 발행 시 throw), 셰이더 프리웜, `window.__harness`, 프레임 통계 기록기, **동결 `surfaces.js`(원본 sha 동일)** |
| `src/physics` | binned-SAH BVH(삼각형 수프), 보존적 전진 스윕 캡슐(터널링 없음), Quake식 평면 스택 collide-and-slide 캐릭터 컨트롤러(스텝오프셋·경사·천장), 6-DoF 임펄스 강체 + CCD + 슬립, 동결 계약 어댑터(`surface-registry.js`) |
| `src/player` | 이동 상태머신(걷기 4.2 / 질주 6.7 / 앉기 2.2 m/s, 점프, 공중 제어), 입력(키보드/마우스/하네스 주입 동일 경로) |
| `src/world` | 90×90m 관아 경내 그레이박스 — ARCHITECTURE §5 공간 문법 7종 전부(마당·대청·방×2·회랑·담장·누각·연못) + 석등·범종·등롱 + 동적 상자 3. 전 정적 메시 표면 타입 태깅 + BVH 등록 (3,508 tris / 1,465 nodes / 빌드 55ms) |
| `src/render` | P0 최소 파이프라인: WebGL2, sRGB, 태양 그림자(PCF 2048) + 반구광 + 등롱 포인트라이트 2(항상 존재 — 라이트 수 변화로 셰이더 순열이 갈라지지 않게). HDR/CSM/TAA/AgX는 P3 |
| `tools` | 하네스 6툴 전부 + `shots.js`(11샷 단일 진실 — 브라우저·Node 공용 import) |

무기·FX·AI·오디오·UI·머티리얼은 P2~P5. 런타임 의존성은 `three@0.180.0` 단독(빌드 도구 없음 — import map 직서빙).

## 2. 하네스 — 6개 툴과 각각이 잡아낸 실제 문제

전부 P0에서, 게임 코드와 같은 커밋 안에서 구축했다.

| 툴 | 잡아낸 실제 문제 |
|---|---|
| `playtest.mjs` | **이동 모델 평형점 결함**: 질주가 5.0m/s에서 멈춤(마찰-가속 평형 5.09 < 목표 6.7). 지수 접근 모델로 교체 후 실측 6.6m/s. 또한 x=0 직선 동선이 석등(0,16)에 정확히 막히며 충돌 배선을 실증(테스트 동선을 x≈-4.6으로 이동) |
| `baseline.mjs` | **`hanji_pierced` 샷에서 결정적 스크린샷 교착**: Playwright `page.screenshot`의 "안정화 대기"가 lockstep 페이지의 컴포지터 상태와 충돌해 2회 연속 같은 샷에서 30s 타임아웃. CDP `Page.captureScreenshot` 직접 호출로 교체(158ms) |
| `imagediff.mjs` | (감사에서) **문서화된 공백 인자 형식(`--tolerance 0`)이 조용히 tolerance=1로 실행** — ±1 LSB 이동이 전부 통과하는 게이트 약화. 파서 교체 + 값 검증으로 수정, 수정 자체의 픽셀 중립성을 이 툴로 재증명 |
| `profile.mjs` | **히치 귀속 인덱스 오프바이원**: `frameTimes[i]`(레코드 i↔i+1 간격)와 프로그램 카운트 차분의 정렬 오류 — 귀속이 이 툴의 존재 이유라 치명적. 수정. 이후 실측으로 "플레이 중 셰이더 컴파일 0" 확인(프리웜 유효성 입증) |
| `capture.mjs` | 그레이박스 구도·조명 육안 확인 경로 (초기 샷 앵글 조정에 사용) |
| `shotset.mjs` | 재현성 없음이 정의인 리뷰 전용 — 게이트 사용 금지를 코드 주석·보고서에 명시 |

**결정성 감사(자동)**: `performance.now`는 `core/clock.js` 내부에만(전 소스 grep), `Math.random` 0건,
rAF 타임스탬프는 메인 루프→clock 경유만.

## 3. 성능 표

P0는 최적화 전 상태 하나뿐이므로 "전/후"가 아니라 **P0 실측**을 기록한다.
(최적화 전/후 비교는 P5 보고의 몫.)

**측정 환경 주의 (CONTRACT-NOTES B5):** 본 개발 컨테이너에는 GPU가 없어
SwiftShader(소프트웨어 렌더러, Vulkan/Subzero)로 측정했다. 절대 fps는 목표 판정에
쓸 수 없고, 이 환경에서 유효한 게이트는 **재현성·회귀·셰이더 컴파일 0·부팅**이다.
§6 fps 목표(p50 ≥ 45)는 목표 하드웨어(Apple Silicon)에서 재측정해야 한다.

`node tools/profile.mjs --duration 30 --dpr 2 --runs 3` (1512×982 @DPR2 = 5.94MP, 이동·점프 스크립트 재생):

| 지표 | P0 실측 (SwiftShader) | §6 목표 (실 GPU 기준) |
|---|---|---|
| fps p50 (중앙값 of 3 runs) | 1.8 | ≥ 45 — **이 환경에서 판정 불가** |
| fps p99 | 0.7 | ≥ 25 — 동상 |
| 최악 프레임 | 2,964 ms | ≤ 50 ms — 동상 |
| **플레이 중 셰이더 컴파일** | **0** (3 runs 전부) | **0 — 달성** |
| **부팅 (ready까지)** | **331 ms** (중앙값; DPR1 fixed 모드 604ms) | ≤ 4 s — **달성** |
| 삼각형 | 3,508 (전 샷 ≤ 6M) | ≤ 6M — **달성** |
| WebGL 프로그램 | 4 (전부 프리웜에서 컴파일, ~400ms) | — |

**P0 종료 조건 3종 (전부 통과):**

| 게이트 | 결과 |
|---|---|
| `baseline.mjs` 11샷 2회 연속 비트 동일 (DPR 2) | **통과** — `imagediff` identical:true, exit 0. (DPR 1에서도 별도 2회 비트 동일 확인) |
| `playtest.mjs` | **통과** — 31체크 (NaN·침투·무한낙하 0, 이동/충돌/점프/앉기/강체 안착 검증) |
| `profile.mjs` p50/p95/p99 + 프레임당 프로그램 카운트 출력 | **통과** — 히치 귀속(신규 프로그램 수·드로우콜·삼각형·직전 이벤트) 포함 |

## 4. 정직한 평가

- **fps 목표는 검증되지 않았다.** 이 환경(소프트웨어 GPU)에서 절대 성능은 원리적으로
  측정 불가라 §6의 p50≥45는 미확인 상태다. 근본 원인(하드웨어 부재)은 알지만 이
  환경에서는 고칠 수 없다 — 목표 하드웨어에서 `profile.mjs`를 그대로 실행하면 된다.
- **profile의 통계 품질이 이 환경에서 낮다.** 30초 벽시계에 ~50 프레임밖에 수집되지
  않아(0.5~2fps) p99가 사실상 최악값과 같다. 히치 카운트 900은 "모든 프레임이 50ms
  초과"라는 뜻으로, 소프트웨어 렌더의 상수 특성이지 스톨이 아니다(신규 프로그램 0으로 귀속 확인).
- **사격 스모크는 아직 없다.** playtest 계약의 "이동·사격" 중 사격은 무기가 생기는
  P2에서 추가된다. 현재는 이동 계열만 검증한다.
- **`viewmodel_ads` 샷은 카메라 포즈만 존재한다.** 뷰모델이 없으므로 P2 전까지
  "20배 조도" 함정 감시는 개점휴업 상태다. 샷 자체는 P0부터 게이트에 포함해 뒀다.
- **그레이박스의 육안 품질은 목표가 아니었고 실제로 낮다.** 하늘색이 단색이라 정오
  샷이 밤처럼 읽히고, 회랑 지붕 하면이 완전히 검다. P3(sky·머티리얼·톤매핑) 전까지
  의도된 상태다 — §6 비평가 루프는 P1 종료부터 돌리는 것이 유의미하다.
- **컨테이너 일시중지가 백그라운드 계측을 죽인다.** 게이트 체인이 한 번 vm fork로
  중살됐다(exit 144). 재실행으로 해결했지만, 장시간 계측은 분할 실행이 안전하다.
- **감사가 부분 실패했다.** 독립 감사 팬아웃 10에이전트 중 8이 세션 사용 한도에
  걸렸다. 완료된 2렌즈(계약 준수·물리 이식)의 발견 5건은 전부 수기 검증 후 수정했지만,
  결정성·하네스 수치·게임플레이 렌즈는 파인더가 완주하지 못해 **커버리지 공백**이
  남아 있다(발견 티저 2건은 실패 목록에서 역추적해 수정). P1 착수 전 재실행 권장.

## 5. 프로세스 노트

- **순차 단일 오너로 진행했다** (§4.1 준수). 병렬화는 읽기 전용 감사 팬아웃 1회뿐
  (결합되지 않은 관심사 — 코드 수정 없음). P0 안에서 병렬 대 순차의 신규 실측 비교는
  없다 — 비교 데이터는 참조 레포의 것(+0.46/결함 증가 vs +1.00/66→26)을 그대로 신뢰했다.
- **참조 레포(Claude of Duty)에서 직접 이식한 것**: `src/physics/math.js`(그대로),
  `bvh.js`·`character.js`·`rigidbody.js`(표면 매핑만 동결 계약 어댑터로 교체),
  lockstep 캡처 기법(페이지 자체 루프 없음), 프리웜의 "실프레임 렌더 필요" 지식,
  툴 CLI 구조. **이식 충실도는 토큰 단위 diff 감사로 검증** — 의미 변화 1건 발견
  (기본 표면 인덱스 0이 'concrete'→'HANJI'로 뒤바뀌는 잠복 결함) 후 수정.
- **측정을 무효화시킬 뻔한 발견들**:
  1. `page.screenshot`의 안정화 휴리스틱이 lockstep 페이지와 교착 — 캡처 자체가
     게이트를 못 돌게 만들었다. CDP 직접 캡처로 우회.
  2. 문서화된 CLI 형식(공백 구분)이 미지원이라 **계약 문서의 명령을 그대로 치면
     profile이 1초/1회/DPR1로 조용히 성공 출력**했다 — 성능 판정 전체가 무효가 되는
     경로. 파서를 계약 형식에 맞추고 실행으로 재검증.
  3. 히치 귀속의 오프바이원 — "어느 프레임에서 컴파일이 일어났나"가 한 프레임씩
     밀려 있었다. 지연 컴파일 스톨 사냥이 목적인 툴이라 즉시 수정.
- **수정의 픽셀 중립성은 주장하지 않고 증명했다**: 감사 수정 5건 적용 후
  `baseline.mjs` 재캡처(계약의 공백 구분 인자 형식으로 실행) → 수정 전 baseline과
  `imagediff` **11샷 비트 동일**(identical:true, exit 0). playtest도 31체크 재통과.
- **계약 위반을 계약으로 잡았다**: world→physics 직접 import(ARCHITECTURE §3 위반)를
  감사가 발견, 주입 파사드(`physics.layers`, 표면 이름 문자열)로 교체.

---

## P1 착수 조건

P0 종료 조건 3종 통과 보고 완료. P1(한옥 모듈 킷, 회색 유지)은 지시 대기.
